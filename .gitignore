*.pyc
*.pyo
/.tox
/lark.egg-info/**
/lark_parser.egg-info/**
tags
.vscode
.idea
.ropeproject
.cache
.mypy_cache
/dist
/build
docs/_build
docs/examples
docs/sg_execution_times.rst

# Ephemeral sub-agent worktrees created by `isolation: worktree` sub-agents
# (sprint/kaizen orchestration). Never repo content — without this the git-check
# stop hook flags them as untracked on essentially every turn.
.claude/worktrees/
