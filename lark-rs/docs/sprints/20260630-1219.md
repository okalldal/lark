# Sprint 20260630-1219 — residue ledger

**Base `master` SHA:** `815408f32aa966874dcb3da5d5d45b41051d78b0`
**Integration branch:** `sprint/20260630-1219`
**Omnibus PR:** (filled at draft creation)

This is the append-only **residue file** (ADR-0023): the irreducible state with no
other durable home — the orchestrator's and review sub-agents' `RETRO:` bullets, and any
synced-`master` SHA. Worker `RETRO:` notes live in their child PR bodies; parked memos on
the issues; follow-ups as filed issues — those are **not** duplicated here.

The authoritative durable record is GitHub itself (the kept branch's merge history, the
child PRs, the labels). The omnibus body is reconstructed from those at finalize (§7); it
is not maintained per-stage.

---

## Synced `master` SHAs

- base: `815408f` (sprint start)

## Retrospective (orchestrator + review sub-agents)

(append below as the sprint runs)

### Wave 1

- **#454 → PR #558 (auto): STAGED** at sprint tip `556529f`.
  - Review RETRO: issue #454's audit table had a cosmetically garbled row label (`A(?=b)` for what was `\101(?=b)`); PR table corrected it — don't propagate the garbled label into the omnibus close-out. `\N{name}` width pinned via raw-source walk (can't build `PatternRe` today; categorized `InvalidRegex`, tracked #461) — sound split.
  - Orchestrator note (kaizen candidate): the pre-push `cargo test --all` gate churns `lark-rs/python/Cargo.lock` (adds `regex-syntax`) in the shared checkout; had to `git checkout --` it to keep the sprint seed clean. A committed-lock drift worth a gate/normalize.

- **#545 → PR #559 (auto): STAGED** at `fef6b8a`. Review RETRO: none. Threshold independently re-grounded vs Python re MAXREPEAT 0xFFFFFFFF; compliance 512/512, scanner-differential 0.
- **#416 → PR #560 (escalate): STAGED** at `89cfe8e`. Review RETRO: none. Faithfulness verified vs lark==1.3.1; public import surface preserved; WASM/C meta untouched (#244 held). ADR-0036 (Proposed). Architect ratifies via omnibus.

### Kaizen candidates accrued (file at close-out per §9 / ADR-0032)
(worker RETROs also persist in their child PR bodies #558/#559/#560)
- K1: worktree isolation — Read/Edit/`cd` default to the *shared* checkout, not the agent worktree; both #454 and #416 workers wasted a cycle. Suggest worker-brief hint: always anchor on the absolute worktree path for Read/Edit and bash `cd`.
- K2: `/code-review` skill spawns finder agents as *background* tasks while the sprint worker brief says "await synchronously/foreground" — framing mismatch (#545 worker). Reconcile the brief wording or the skill.
- K3: decisions index `README.md` silently rots — ADR-0033 & ADR-0035 existed on disk but were unindexed (found by #416 worker). Suggest a CI check that every `NNNN-*.md` has an index row.
- K4: PyO3/abi3 cannot subclass native `str` — future binding `str`-subclass triage should note this up front so blast-radius (pure-Python realisation + module rename) is estimated correctly.
- K5: pre-push `cargo test --all` gate churns `lark-rs/python/Cargo.lock` (adds `regex-syntax`) in the shared checkout — orchestrator had to discard it. Worth a gate/normalize.

### Wave 2 (partial — #367 still in flight)

- **#493 → PR #562 (auto): STAGED** at `9b7b139`. Correctness review surfaced an out-of-scope divergence (explicit-`!` imported-extend over-keep) → split to follow-up **#561** (independently confirmed a correct scope-split: #493 under-keep vs #561 over-keep, opposite directions). Review RETRO: #561 lands untriaged mid-sprint (expected; close-out triage must sweep it); test fn names still reference predecessor #442 (cosmetic, non-blocking).
- **#478 → PR #563 (auto): STAGED** at `00c5d37`. CLEAN PURE MOVE — 3 recognizers kept separate (architect decided against unify), "do NOT unify" comment verbatim, `width_range` stays `pub(crate)`, 14-test block moved verbatim. Review RETRO: none.

### Orchestrator RETRO / kaizen (Wave 2)
- **K6 (strong, recurring): worker + review sub-agents strand on background waits.** BOTH the #478 and #493 workers, AND the #563 verdict-only reviewer, spawned their `/code-review`/gate/diff-audit steps as *background* sub-agents and then ended their turn idle ("I'll wait for the monitor notification") — exactly the anti-stranding clause in the worker brief, violated three times in one wave. The committed/staged work was left unpushed (one worker hadn't even committed). The orchestrator recovered each: pushing worktree branches, committing staged-but-uncommitted splits, opening the child PRs, and resuming the reviewer with its own subagent's result. This is K2 escalated: the brief's "await synchronously (foreground)" is not honored because `/code-review` (and Agent) default to background and the harness ends the turn while idle. Suggest: the sprint worker/review brief must FORBID background review entirely (run findings inline), or the kit must make `/code-review` foreground-blocking. High-value kit fix.
- **K7:** a mid-sprint follow-up (#561) is filed with only `lark-rs`,`kind:bug` — no `prio:`/`status:triaged`. The §9 close-out / next `/triage` must sweep newly-filed sprint follow-ups so they don't sit unscheduled.

### Wave 3

- **#515 → PR #565 (escalate/docs): STAGED** at `82da901`. CLAUDE.md-only reword of the within-terminal arm-sort note to the match-width key `(-max_width, -min_width, -len(value))` citing `sort_terminal_arms`/`TerminalTreeToPattern` (both resolver + `%extend` paths share it). Verdict-only review: in scope, no governance/policy text, CI 4/4.
- **#377 → PR #566 (auto): STAGED** at `93e5eeb`. Filter-out-agnostic `RecurseShareKey` shares the `+`/`*` recurse helper across opposite-order unified-terminal sites (first-defined-wins `filter_out`, matching Python); amends **ADR-0013 (Amendment 3)**. LALR bank stayed **512/512**, RC7/#272 differentials green — the new key is strictly coarser (merges only opposite-`filter_out` variants), share/split decision unchanged.
- **#349 → PR #567 (auto + ADR-0037): STAGED** at `9299a19`. Hybrid-DFA fallback (`CombinedDfa::Hybrid`) for over-budget terminals: `build_partitioned_dfa` probes each source under a 64 KiB `dfa_size_limit` and routes genuine size-overflow sources to a parallel lazy DFA, keeping `dense_build_bytes` flat per source (the `.*a.{N}` family no longer hangs; byte-identical, oracle parity preserved). XFAIL `h4_12_dense_dfa_build_is_subexponential` flipped. Out-of-scope review find → **follow-up #568** filed (guard-body DFAs in `guard.rs::build_anchored_dfa` not yet budget-gated — latent, not a live regression).

### Orchestrator RETRO / kaizen (Wave 3)
- **K8 (recurring infra): container restarts kill in-flight worker agents.** The container restarted twice during Wave 3. The #349 worker died on the first restart with **no commit** (worktree still at the base SHA) and had to be re-dispatched fresh — one wasted dispatch + orchestrator recovery (prune dead worktree, delete the stale base-only branch, re-dispatch). The #377/#515 workers had already pushed + opened their child PRs before the restart, so they survived intact. A restart also left the **main checkout stranded on a worker's branch with an uncommitted file**, needing a hard reset to the sprint tip. Mitigation that worked: every staged child PR is squash-merged into the kept integration branch immediately on green, so restart-surviving state == the branch history. Suggested kit fix: instruct workers to commit+push early (even a WIP commit) so a mid-flight restart loses minimal work.
- **`send_later` hour-out heartbeat requires interactive approval** in this session, so the sprint could not self-schedule a recovery check; relied instead on harness worker-completion notifications + the per-child-PR activity subscriptions (which auto-fire on merge). Worked, but means a *silent* worker death between notifications has no backstop — couples to K8.
- **#568** filed with `lark-rs`,`performance`,`kind:bug` only (no `prio:`/`status:triaged`) — same gap as K7; the close-out `/triage` sweep must cover both #561 and #568.

---

## Close-out (§7)

All 9 schedulable `good-autonomous` issues staged onto `sprint/20260630-1219`; the
omnibus PR #557 body was reconstructed (full Staged table + `Closes #N` lines) and
taken out of draft for architect review. **No automation merged to `master`** (ADR-0018).
Newly-filed sprint follow-ups awaiting triage: **#561** (Wave 2), **#568** (Wave 3).
