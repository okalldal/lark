# Sprint 20260622-2013 — residue ledger

**Sprint base (`master`):** `04017a2dfa55438a8a928eb97433f50fc4c084a0`
**Integration branch:** `claude/start-sprint-qxqxu0`
**Omnibus PR:** #284

This is the append-only **irreducible residue** for the sprint (ADR-0023): the
orchestrator's and review sub-agents' `RETRO:` notes, and any synced-`master` SHA.
It is **not** the source of truth for staging — that is reconstructed at finalize
from the integration branch's merge history + child PR bodies. Worker `RETRO:`
notes live in their child PR bodies; parked memos live on the issues.

## Schedulable plan (locked)

All 15 are `good-autonomous`, not blocked / needs-decision / in-progress, with a
falsifiable done-when (most pin a named XFAIL) + identified fix site. Waves are
grouped by blast-radius overlap (≤3 concurrent), serializing same-file issues.

- **Wave 1:** #268 (lexer/plan.rs + grammar/terminal.rs), #269 (grammar/loader/parser.rs),
  #272 (parsers/lalr.rs)
- **Wave 2:** #258 (grammar/loader/ebnf.rs), #276 (lexer/dynamic.rs),
  #277 (parsers/tree_builder.rs)
- **Wave 3:** #271 (ebnf.rs + parser.rs — after #258/#269 stage), #274 (lexer/classify.rs+route.rs),
  #256 (parsers/earley.rs + cyk.rs)
- **Wave 4:** #279 (ebnf.rs compile_repeat — after ebnf cluster), #270 (grammar/loader/compiler.rs),
  #273 (parsers/mod.rs)
- **Wave 5:** #278 (lexer/mod.rs LexCursor), #280 (standalone/mod.rs),
  #265 (tools/generate_oracles.py + tests/test_interactive.rs)

#268–#280 are the bug-bounty XFAIL burndown (epic #282); each flips a named XFAIL and
references `lark-rs/docs/BOUNTY_FINDINGS.md` / `BOUNTY_FINDINGS_H2.md`.

## Not sprint-schedulable (close-out memo)

- **needs-decision (architect inbox):** #230, #242, #243, #244, #253, #275.
- **blocked:** #233, #234 (on C7 / #232); #242/#243/#244 (also needs-decision).
- **epics / umbrella trackers:** #208, #209, #225, #282.
- **not good-autonomous / deferred:** #69, #164, #165 (phase-3 prio:later feat),
  #232 (C7 public API; body says blocked on #231), #281 (provisional bug — needs repro).
- **already-claimed (open PR):** #253 → PR #264 (open, targets master).

## Parked (needs-decision / blocked)

- **#272** (RC7 LALR reduce/reduce) — *parked `needs-decision`* (worker, wave 1; no
  child PR). RC7 is real but the issue's named fix site (`parsers/lalr.rs` conflict
  detector) is wrong: the detector is correct; lark-rs's EBNF `recurse_cache` shares one
  `__anon_plus` helper across `r0*` and `(r0)*` (**ADR-0013, Accepted**) so the two
  reduce candidates never co-occur, whereas Python mints two colliding helpers. Naive
  un-share regresses the LALR bank **512→482 (−30)** — sharing is load-bearing. Fork
  (architect): (A, rec) keep sharing + post-lowering collision audit; (B) narrow the
  cache key surgically; (C) diverge-and-document/wontfix. Memo on #272; full writeup in
  **escalation issue #285**. Labels: removed `good-autonomous`, added `needs-decision`.

## Synced-`master` SHAs

(none yet)

## RETRO (orchestrator + review sub-agents)

- **(worker #272, no child PR → recorded here)** (a) Issue body mis-located the fix site
  (`parsers/lalr.rs` conflict detection) — actual divergence is upstream in
  `grammar/loader/ebnf.rs` helper sharing (ADR-0013). A worker trusting the stated site
  would churn the wrong file. Suggest `/triage` (or the bug-hackathon filer) verify the
  named fix site against a quick repro before labelling `good-autonomous`. (b)
  `mcp__github__issue_write` `labels` must be a JSON array (`["a","b"]`), not
  comma-separated — first call coercion-errored. (c) ToolSearch `select:` for already-loaded
  github MCP tools returned "no matching tools" yet the tools were callable — search seam
  didn't resolve pre-loaded tools (no blocker).
- **(orchestrator, wave 1)** #272's escalation produced a *new* issue #285 alongside the
  parked originating issue #272 — both now `needs-decision`. Close-out memo lists the fork
  once (decision tracked on #285, bug on #272) to avoid double-counting.
- **(review #287 → recorded here)** DoD PASS, tier `auto`; staged as squash `(#287)`.
  One non-blocking find: a code comment in `stage_term_directive` (compiler.rs) said the
  imported-terminal `%extend` gap was "tracked in #287" (the PR's own number) — should be
  #286. The PR/test docstrings were correct. Fixed on the integration branch directly
  (sanctioned one-line fixup). Quirk: worker drafted the comment before the follow-up
  issue (#286) and child PR (#287) numbers were known relative to each other — file the
  follow-up issue *before* referencing it.
- **(orchestrator, wave 1 — KIT BUG, worker stranding)** The #268 worker did the full
  implementation (regex-syntax HIR max-width + raw_value_len, both XFAILs green, validated
  by its OWN two review sub-agents) but **came to rest WITHOUT committing/pushing/opening
  the child PR** — it ended its turn at "awaiting the review agents" while its spawned
  review sub-agents were still running (their notifications arrived *after* the parent
  rested). `SendMessage`-to-resume was not available. Recovered by: inspecting the on-disk
  `isolation: worktree` checkout, confirming RC5+N2 pass, committing+pushing the branch
  (`work-268-terminal-ordering`), and dispatching a thin **finalize-only** worker to
  rebase/gate/open-PR. **Lesson:** the worker brief's "LAST action must be push→PR→return"
  is insufficient when a worker spawns background review agents and then ENDS ITS TURN to
  wait on them. The brief must say: *do not end your turn while your own review sub-agents
  run — await them synchronously (foreground), then push→open-PR→return in the same turn.*
  Likely a kaizen item for the start-sprint worker brief. (Same class as the prior sprint's
  "worker stopped after /code-review" RETRO — recurring failure mode.)
