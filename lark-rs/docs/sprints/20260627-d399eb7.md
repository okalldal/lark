# Sprint 20260627-d399eb7

- **Sprint base (master SHA):** `d399eb7`
- **Integration branch:** `claude/start-sprint-4bd31u`
- **Omnibus PR:** _(filled at open)_
- **ADR-0023 residue ledger** — append-only. Source of truth for orchestrator/review `RETRO:` notes and synced-`master` SHAs. The *Staged* table is reconstructed at finalize from the branch merge history (§7); it is NOT maintained live here.

## Schedulable plan (from §3 triage)

| # | Title | Files | Wave |
|---|-------|-------|------|
| 500 | Cross-engine propagate_positions empty-span divergence | tree_builder.rs + lalr/cyk/earley | 1 |
| 527 | apply_pending_interior_extends checked_sub hardening | loader/compiler.rs | 1 |
| 529 | standalone Meta Default empty:true + docs | standalone/runtime.rs | 1 |
| 509 | normalize_python_escapes strips (?#…) inside {…} | terminal.rs | 2 |
| 510 | quantifier on bare anchor accepted (nothing to repeat) | terminal.rs | 2 |
| 501 | unify two regex-screen sibling walkers (RegexCursor) | terminal.rs | 3 |
| 528 | soften Pattern hash test to Eq/Hash consistency | terminal.rs | 3 |

NEEDS_DECISION (24), BLOCKED (4), triage-repair-needed (4: #454/#493/#517/#518) — not scheduled.

## RETRO ledger (append-only)

**Wave 1 reviews (#531/#532):** `RETRO: none` from both verdict-only reviewers.

**Worker #527 (PR #531) RETRO** (captured here — not in the PR body):
- Worktree lives on a different path than the shared checkout (`.claude/worktrees/agent-…`); first Edit targeted the shared-checkout path and was rejected by an isolation guard. Suggest front-loading the worktree `git checkout` + first Read against the worktree path before any context-gathering Reads (relates to #311/#314 worktree friction).
- A PostToolUse formatter hook rewrote the multi-line `checked_sub`/`ok_or_else` block and dropped an intended trailing `Ok(())`, caught only on re-read. When changing a fn return type to `Result`, re-read the whole body after the formatter pass.

**Worker #509 (PR #536) leak (kaizen):** worker left a scratch cargo build log `lark-rs/.t536.log` in the SHARED checkout (not its worktree) — the #311 leak class again. Orchestrator removed it; tripped the stop-hook untracked-files check. The "keep ALL files incl. scratch inside your worktree" clause is still being violated; consider a worktree-local-only enforcement or a `.gitignore` for `*.log`/`.t*` scratch patterns.

**Worker #509 (PR #536) finding:** issue #509 was already fixed on the sprint branch by sibling #462; the right deliverable was pins + differential audit, not a code change. Worker filed follow-up **#534** (inverted-bound `{3,2}` over-acceptance) as a product follow-up. (Resume note: re-verify "file-and-hold" issues against the current oracle before assuming a code change is needed.)

**Wave 2 reviews:** #535 (#510) PASS/auto `RETRO: none`; #536 (#509) PASS/auto (RETRO: ENOSPC disk-full mid-review, reclaimed incremental cache — infra). #537 (#500) PASS/auto (RETRO: differential-audit breadth lives in PR-body prose by design; only the *pins* are permanent — reviewer can re-verify pins + regen idempotency, not re-run the audit).

**Worker #500 (PR #537) RETRO** (captured here — not in PR body):
- #500 was a NO-REPRO at current tip (incidentally fixed since baseline `8d34232` by #402/earley-walk-split). Deliverable = regression net (13 tool-generated oracle cases + cross-engine & streaming-vs-assemble pins) + ~440-case differential audit (0 divergences), fail-first injection proved teeth. No engine/builder source touched.
- Re-dispatch friction: dead attempt left a *locked* worktree holding the branch name (stale pid from restarted container); `git worktree remove -f -f` after confirming pid dead was the unblock. Future re-dispatch packets should warn about this.
- Isolation-guard path resolution: Read/Edit resolved to the shared checkout even when passed the worktree path; workaround = write new files via Bash heredoc + Read worktree path before Edit. (Same class as worker #527/#510 notes — recurring, worth a kit fix.)
- ENOSPC: disk hit 100% from multiple concurrent worker `target/` dirs (~4.5G each). **Reinforces existing needs-decision #313** (recurring shared-disk ENOSPC from concurrent worktree target/). Three separate workers hit this in this sprint.

**Worker #510 (PR #535) RETRO** (captured here): `git checkout -b` ran against shared checkout but Edit needs the worktree path (no harm, branch already in worktree); scratch `include_str!` needed `../../` path-guessing — placing scratch under `lark-rs/` would avoid it.

**Orchestrator RETRO (running):**
- Container restarted mid-wave-1: worker #527 returned cleanly; worker #529's child PR (#532) was fully formed but the worker was killed before delivering its return message (recovered by reconstructing from GitHub per ADR-0023); worker #500 was killed mid-differential-audit with no branch pushed (re-dispatched). Resume-from-GitHub worked as designed.
- `mcp__github__list_issues` and `mcp__github__actions_list` outputs overflow the tool-result token cap (168k / 354k chars) — had to slice via python / parse in a subagent. Kaizen: a thinner listing mode or server-side filter would help (cf. #314 family).

## Synced-master SHAs

_(none yet)_
