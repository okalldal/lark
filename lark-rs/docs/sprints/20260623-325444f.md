# Sprint 20260623-325444f — residue ledger

**Sprint base (`master`):** `325444f5c0a16a284b362289194b6f97402b3053`
**Integration branch:** `sprint/20260623-325444f`
**Omnibus PR:** #354 (draft)

This is the append-only **irreducible residue** for the sprint (ADR-0023): the
orchestrator's and review sub-agents' `RETRO:` notes, plus any synced-`master` SHA.
The authoritative durable record is GitHub itself — the kept integration branch's
merge history, the child PRs, and the labels. The *Staged* table is reconstructed at
finalize from the branch's merge history; it is **not** maintained here per-stage.

## Schedulable plan (22 issues)

Waves grouped by blast-radius (fix-site file overlap); ~3 concurrent workers/wave.

| Wave | Issues | Primary fix files (disjoint within wave) |
|------|--------|------------------------------------------|
| 1 | #330, #348, #334 | intern.rs · earley.rs · parsers/mod.rs |
| 2 | #346, #335, #343 | parsers/lalr.rs · lexer/dynamic.rs · loader/imports.rs |
| 3 | #331, #333, #336 | loader/compiler.rs · grammar/terminal.rs+lexer/route.rs · standalone/ |
| 4 | #347, #342, #298 | intern.rs+loader/terminals.rs · grammar/terminal.rs · standalone/mod.rs |
| 5 | #289, #344, #345 | intern.rs+lalr · loader/tokenizer.rs · loader/compiler.rs |
| 6 | #351, #352, #302 | loader/compiler.rs · loader/tokenizer.rs+terminal.rs · earley.rs |
| 7 | #353, #288, #293 | loader/compiler.rs · grammar/terminal.rs · test-grammar/oracle infra |
| 8 | #299 | loader/compiler.rs+imports.rs |

Skipped (claimed by open PR): #337 (PR #339), #286 (PR #328).

## Synced master SHAs
(none yet — base is 325444f)

## RETRO ledger (orchestrator + review sub-agents)

- **[orchestrator, Wave 1] #326 recurred (worktree worker operated in the shared checkout).**
  The first #348 worker created its work branch `fix/348…` and a scratch
  `examples/h4_10_dump.rs` in the **shared checkout** (`/home/user/lark`) — its own
  `isolation: worktree` working tree stayed clean at the base commit. This switched the
  orchestrator's shared checkout off the sprint branch and tripped the Stop-hook
  untracked-file check. Recovery: TaskStop the worker, `git checkout` the sprint branch,
  `rm` the stray file, `git branch -D fix/348…`, re-dispatch with a hardened "Worktree
  step 0" (explicit `pwd` assertion + "never `cd /home/user/lark`"). This is exactly the
  failure #326 is filed to fix; the standard worker brief's worktree-step-0 wording is
  **not** strong enough on its own. Suggest #326 land a mechanical guard (a worker
  pre-flight that asserts `pwd` is the worktree, or a Stop-hook that rejects shared-tree
  edits from worktree agents).

- **[review, #359/#335] RETRO: none.**

- **[worker→ledger, #335] kaizen (KIT):** adding a 2nd `#[test]` touching the process-global
  perf-counter atomics races under the parallel runner → confusing "recorded zero scan steps"
  masking as a fix bug. Fixed with a shared `Mutex`. Suggest a doc-note in the perf-counter test
  module: *any* new perf-counter test must serialize. Also: `regex::Regex` has no anchored-at-pos
  search; canonical pattern is `regex_automata::meta::Regex` + `Input::span().anchored()` (as in
  `dfa.rs`) — worth documenting for future `find_at` ports.

- **[orchestrator, Wave 2] background workers did not survive a session resume.** The first
  Wave-2 batch (#348/#346/#335) stalled: all agent jsonls froze at the same instant and `TaskStop`
  reported "no task found" → the background sub-agents were torn down with the session lifecycle.
  Verified via GitHub (no child PRs) + worktrees (no commits, nothing pushed) that **zero work was
  lost**, pruned 5 stale worktrees, and re-dispatched. Kaizen: a long-running sprint needs a
  resume-safe way to detect dead-but-unfinished workers (the fallback heartbeat covers this, but a
  first-class "list live background agents" signal would be better). Re-dispatch from GitHub state
  worked exactly as the ADR-0023 resume protocol intends.

- **[worker→ledger, #348] PARKED as needs-decision (escalate-tier architecture fork).** Confirmed
  the fix site is the explicit-mode forest→tree enumeration in `parsers/earley.rs` (NOT the
  hypothesised `node_value_key` dedup): lark-rs's 6 derivations of `"bbb"` are a strict subset of
  Python's 8, SPPF structurally identical, divergence purely in cycle-aware enumeration. lark-rs
  discards a family on any back-edge re-entry; Python's `ForestToParseTree` uses context-sensitive
  `_successful_visits`/`_check_cycle` + per-visit cache. A faithful fix replicates Python's
  context-sensitive visitor — at odds with lark-rs's deliberately memoized, de-recursed O(1)-stack
  walk (#33/#54/#55/#59/#159). Three bounded approximations all fail the oracle. Memo + labels
  (`needs-decision`, `good-autonomous` removed) posted on #348. kaizen (KIT, #326-family): worktree
  Edit was rejected when using the bare repo path — workers must use absolute worktree-prefixed
  paths for Read/Edit, not the shared `/home/user/lark` path.

- **[worker→ledger, #336] kaizen (KIT):** (1) the issue's Done-when said run the XFAIL with `--ignored`,
  but un-ignoring it (the natural way to make it a permanent regression pin) means `--ignored` then
  filters it out (0 tests) — the `--ignored` invocation in issue text goes stale once the XFAIL flips;
  worth a triage-template note. (2) This worker again completed fix+review but ended its first turn
  BEFORE the atomic push→PR step and had to be rescued by the orchestrator (SendMessage resume) —
  a recurrence of the #309 stranding despite the brief's anti-stranding clause. The clause is **still
  not strong enough**; suggest a mechanical Stop-hook that refuses to end a worker turn with
  committed-or-modified-but-unpushed work in the worktree.
- **[orchestrator] staging progress:** staged #330(#355), #334(#356), #335(#359), #346(#369),
  #336(#371). Parked #348 (needs-decision). In flight: #343, #333, #331.

- **[worker→ledger, #333] kaizen (KIT, serious):** a spawned **review sub-agent ran `git checkout
  src/grammar/terminal.rs` in the shared worktree and blew away the worker's UNCOMMITTED diff.** The
  worker detected it, reconstructed every change, and recovered — but it cost a full re-apply pass.
  Two fixes: (1) review/finder sub-agents are **read-only** and must NEVER mutate git state (no
  `checkout`/`reset`/`stash`); the review brief should say so explicitly. (2) Workers should **commit
  early** (before launching any reviewer) so a stray reset can't strand the diff. Also: running probe
  code by appending a `#[cfg(test)]` module to the file under edit is risky (needed surgical removal +
  `cargo fmt`) — use a git-ignored throwaway under `tests/` instead. (The sprint worker briefs from
  wave 5 on already carry "commit early / reviewers are read-only" — keep that.)
- **[worker→ledger, #344] kaizen (minor):** `add_issue_comment` MCP needs `owner`/`repo` as separate
  args; a worker once passed `okalldal/okalldal` as owner. Document the arg shape in the worker brief.
- **[orchestrator] staging progress:** + #331(#374), #343(#373), #336(#371), #346(#369), #333(#375),
  #344(#376). 9 staged total; #348 parked. In flight: #347, #345, #298.

- **[orchestrator] staging progress (wave 4-6):** + #347(#378), #298(#379), #345(#380), #342(#381).
  **13 staged total**; #348 parked. In flight: #289, #302, #293. Unstarted: #351, #352, #353, #288, #299.
  Product follow-ups filed by workers: #372 (#343 — overlapping closure dedup), #377 (#347 — multi-site
  recurse_cache filter_out keying).
- **[orchestrator] kaizen (KIT, recurring — escalating):** worker stranding hit a THIRD time (#336, #298,
  now #345). #345 was worst: it spawned its pre-PR review sub-agents in the **background** and ended its
  turn awaiting them, so the reviewers reported to the orchestrator instead, AND a stray probe file was
  reported in its worktree. All three recovered via SendMessage-resume, but this is the #309 failure mode
  recurring despite ever-stronger brief wording. **The brief is provably insufficient on its own — file
  the mechanical guard (#324: Stop-hook refusing to end a worker turn with committed-or-modified-unpushed
  work in the worktree; the background-review case also needs a guard).** Mitigation that DID help:
  "commit early before launching reviewers" kept #298/#345 work recoverable (committed, just unpushed).
- **[orchestrator] kaizen (KIT):** verdict-only **review sub-agents keep running `git checkout`/`restore`
  in the shared checkout** despite an explicit READ-ONLY instruction (the #333 worker's reviewer stranded
  the worker's diff; the #378 reviewer ran `git checkout` then self-restored). The instruction is not
  enough — reviewers should be denied working-tree mutation mechanically, or always review purely from the
  GitHub API diff (which they have). Wave-7 review briefs already say "review entirely from the API diff;
  do NOT touch the working tree" — keep and harden that.

- **[orchestrator] staging progress (wave 6-7):** + #289(#382, **ESCALATE**), #293(#383). **15 staged**; #348 parked.
  In flight: #302, #351, #352. Unstarted: #353, #299, #288 (all serialize behind active compiler.rs/terminal.rs workers).
- **[orchestrator] ARCHITECT ACTION — ADR-0033 (Proposed) rides #289/#382:** the root `?start:[A]` fix
  required a new public-API variant `ParseTree::None`. Worker did NOT self-ratify; recorded ADR-0033 Proposed.
  This is the sprint's one **escalate-tier** stage — the architect ratifies ADR-0033 (flip to Accepted) by
  merging the omnibus, or sends it back. MUST be flagged in the omnibus Architect Action Memo (§9).
- **[orchestrator] CLOSE-OUT FOLLOW-UPS TO FILE (product):** #293's oracle-honesty burndown surfaced two
  genuine pre-existing lark-rs lexer divergences (out of scope, flagged-not-filed per the sprint model — the
  orchestrator files at close-out per §2/ADR-0030): (1) IMAG `.2` priority not honored over FLOAT in the
  contextual lexer; (2) prefixed-underscore HEX/OCT/BIN (`0x_`/`0b_`/`0o_`) not matched. Plus the #289 worker's
  note: standalone `?start:[A]` (separate runtime::ParseTree enum, LALR-only) not covered — file if architect wants.

- **[orchestrator] MASTER SYNC (§6) — incident resolved.** During the sprint `master` advanced
  `325444f → 40e3dea` ("test(bounty-h5): 8 fresh oracle divergences as XFAIL tests"), adding
  `tests/test_bounty_findings_h5.rs`. The omnibus PR (#354, base master) runs CI on `master ∪ sprint`;
  master's new h5 test has a `match` over the public `ParseTree` that did NOT cover the sprint's new
  `ParseTree::None` variant (#289/#382) → omnibus `test` went RED with E0004 non-exhaustive-match. This is
  the §6 "master moved during the sprint" case, NOT a bad stage — so per §6 (NOT rollback-first): merged
  `origin/master` (40e3dea) INTO the sprint branch (real merge commit, sprint only moves forward), and
  resolved the interaction inside the sprint by adding the mechanical `ParseTree::None => {}` arm to the
  merged h5 test (a None result carries no tokens, mirroring the file's existing `Child::None => {}`).
  Compiled clean locally (`cargo test --all --no-run` exit 0). **Synced master SHA: 40e3dea.**
- **[worker→ledger, #302] PARKED needs-decision (same fork class as #348).** Confirmed #302 is the Earley
  completer packed-family insertion-order fork: grammar lowering is byte-identical to Python; the divergence
  is purely the completer's family insertion order, and Python's segmentation across nrep×len is non-monotone
  so no local `sorted_families` tie-break matches it (a local heuristic fixed nrep=2 but regressed nrep≥3,
  caught by the worker's foreground read-only review against a baseline build — banks missed it, a coverage
  gap). Memo posted on #302; labels → status:triaged + needs-decision. RETRO (kaizen): when a fix targets an
  N-ary ambiguity, vary N (probe 3+) before concluding a 2-case heuristic generalizes.

## FINALIZE (§7) — reconstruction

**Sprint base:** `325444f` · **Synced master:** `40e3dea` (merged in) · **Final sprint tip:** `6ef12e8`

### Staged child PRs (19), reconstructed from branch merge history
| Issue | Child PR | tier | one-line |
|------|------|------|----------|
| #330 | #355 | auto | undefined start → GrammarError (no panic) |
| #334 | #356 | auto | Earley lexer=auto → dynamic when no postlex |
| #335 | #359 | auto | Earley dynamic lexer anchored → O(n) |
| #346 | #369 | auto | contextual unlexable char → UnexpectedCharacter |
| #336 | #371 | auto | standalone expand1 lone-None collapse |
| #343 | #373 | auto | import-closure sibling left unmangled |
| #331 | #374 | auto | loader build-time validation (template params + terminal alias) |
| #333 | #375 | auto | python-re dialect front-end (H6–H9) |
| #344 | #376 | auto | keep backslash for \v \0 \' in string literals |
| #347 | #378 | auto | collapse equal named-terminal-vs-literal alternation |
| #298 | #379 | auto | standalone bake routed through validate_config |
| #345 | #380 | auto | %ignore NAME marks named terminal (preserves priority) |
| #342 | #381 | auto | reject regex-crate-only \p/\P/\x{}/\z dialect |
| #289 | #382 | **escalate** | root ?start:[A] → bare None; **ADR-0033 (Proposed)** ParseTree::None |
| #293 | #383 | auto | burn down 6 oracle contradictions → empty allow-list |
| #352 | #384 | **escalate** | priority i32→i64; **ADR-0034 (Proposed)** widened public fields |
| #351 | #386 | auto | reject nested optional-of-optional collision |
| #353 | #387 | auto | reject %declare of a rule-cased (lowercase) name |
| #299 | #389 | auto | reject 2 duplicate-definition surfaces |

### Parked needs-decision (3, NOT staged)
- **#348** — nullable+recursive Earley under-reports derivations; escalate-tier completer-family-insertion-order architecture fork.
- **#302** — adjacent bounded-repeat segmentation; SAME fork class as #348 (proved: no local tie-break matches Python's non-monotone order).
- **#288** — raw_value_len global-flag strip: **wont-fix recommended** (3-way differential: fix helps 0 / hurts 24; phantom length accidentally compensates a PatternStr-vs-PatternRE gap).

### Architect ratification needed (escalate)
- **ADR-0033** (rides #289/#382) — accept `ParseTree::None` public variant.
- **ADR-0034** (rides #352/#384) — accept priority `i64` widening of public `RuleOptions`/`TerminalDef`.

### Follow-ups filed during sprint
- **Product:** #372 (#343 overlapping-closure dedup), #377 (#347 multi-site recurse_cache filter_out), #385 (#351 stale rc3 XFAIL), #388 (#299 same-source two-alias over-accept + msg-fidelity + 3-way inconsistency; #390 closed dup), #391 (#293 two lexer gaps: IMAG-priority, prefixed-underscore numerics).
- **Kaizen:** #392 (review sub-agents must be mechanically read-only + foreground; minor doc/triage notes bundled) + reinforced #324 (Stop-hook for unpushed worktree work — stranding recurred 6×).

### Aggregated retrospective (grouped)
- **[KIT, high] worker stranding ×6** (#336/#298/#345/#351/#353/#299): ended turn with committed-unpushed work, mostly via background-default review agents → #324 (reinforced) + #392.
- **[KIT, high] review sub-agents mutate git** despite read-only briefs (#333 reviewer wiped a worker diff; #378/#299 reviewers ran checkout / wrote a stray probe) → #392.
- **[KIT] master moved mid-sprint** → omnibus red on master∪sprint with the new ParseTree::None variant; resolved by §6 merge-master-in + mechanical None arm (synced `40e3dea`). Worked as designed.
- **[KIT, minor]** stale `--ignored` issue wording post-XFAIL-flip (#336); GitHub-ops-via-mcp-not-gh (#352); perf-counter test must serialize (#335); add_issue_comment owner/repo arg (#344) → bundled in #392.
- **[know-how]** vary N before generalizing an N-ary-ambiguity heuristic (#302); run with-fix/without-fix/oracle triple for tiebreak bugs (#288); trust the oracle over issue prose (#347/#331/#293).
