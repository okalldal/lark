# Sprint 20260701-ef5dcd3

- **Sprint base (master SHA):** `ef5dcd3` (CI green at dispatch)
- **Integration branch:** `claude/start-sprint-fi32qk`
- **Omnibus PR:** (draft, opened early)

This is the append-only residue ledger (ADR-0023): the irreducible state with no
other durable home — orchestrator/review `RETRO:` bullets and any synced-`master`
SHAs. The *Staged* table is reconstructed at finalize from the integration branch's
merge history + child PR bodies; it is NOT maintained here per stage.

## Plan (from GitHub label state, §3)

Backlog survey: 67 open issues. Exactly **one** is sprint-schedulable
(`good-autonomous`, not `needs-decision`/`blocked`/in-progress, parseable
Done-when + Files):

- **#234** — Semantic output C9: semantic output in generated standalone parsers.
  Body-stated dependency on C7 (#232) confirmed satisfied — #232 CLOSED/completed
  2026-07-01. One-wave sprint.

Everything else: `kaizen` infra (not `good-autonomous`), `needs-decision`
regex-dialect/frozen findings (architect inbox), or XFAIL/epic trackers — none
schedulable. #568 is a near-miss (has Done-when but no `## Files` block).

Pre-existing open PRs #553 (docs essay) and #533 (SessionStart hook) target
`master` directly and are independent architect PRs — outside sprint scope.

## Staging log (compact — authoritative table reconstructed at finalize)
- PR #585 (#234, tier `auto`): verdict-only review DoD PASS, CI green (4/4), no confirmed
  bugs. Staged into the integration branch via squash-merge.
- PR #586 (#234 follow-up, tier `auto`): architect-requested hardening of the C9 oracle
  gate (`standalone_semantic_output_matches_transformer_oracle`) — missing `cases.json`
  and ok-fixture bake failures now hard-fail; exact `checked == expected` coverage assert
  (20/20, derived not hardcoded). Fail-first proof pinned. Verdict-only review DoD PASS,
  counter placement clean (no off-by-one). Staged via squash-merge.

## Finalize (§7 reconstruction)
- Integration branch merge history reconstructed: one staged child, squash commit
  `faac3a2` = PR #585 (#234, tier `auto`). master (`ef5dcd3`) confirmed ancestor.
- Omnibus #584 body finalized with `Closes #234` + staging table + evidence.
- Architect Action Memo + Durability Warrant posted as comment on #584.

## Synced-master SHAs
- (none — master did not move during the sprint; base `ef5dcd3` == tip at finalize)

## Orchestrator / review RETRO notes

### Worker RETRO (#234 / PR #585) — captured here since not in the child PR body
- The issue's "same action specs as C1/C3" pointed at
  `tests/fixtures/oracles/transformer/cases.json` + `test_transformer_oracle.rs`; the
  non-obvious detail was that the in-process `parse_into` gate compares to the
  **post-parse** `value`/`trace`, NOT the fixture's `embedded` block. A first pass
  comparing against `embedded` fails on the discard fixtures — future runs should reuse
  the in-process `check_case_config_embedded` (post-parse oracle) contract from the start.
- Python `lark` is not installed in the worktree, so `generate_transformer_oracles.py`
  cannot re-verify freshness locally; the committed `cases.json` is the frozen oracle.
  Not a blocker (tests run without Python), but the differential can't be regenerated here.

### Review RETRO (#585 verdict-only)
- PR #585 body was unusually complete (failed-first story, ADR-0038 §2
  Discard/`__default_token__` divergence documented, explicit "no oracle beyond the
  fixture" note) — made the differential-audit checkpoint cheap to adjudicate.
- The child PR diff exceeded the MCP tool token cap and had to be read via a subagent
  from the saved tool-result file. The baked-data-vs-additive distinction was only
  confirmable by verifying the removed lines were solely the re-export widening —
  suggest reviewers lean on `git diff --stat` region locality + "seam bodies
  byte-identical across runtime.rs and both fixtures" as the hand-edit tripwire.
  (Overlaps existing kaizen #435 on large sprint-child diffs overflowing MCP get_diff.)

### Worker RETRO (#586 hardening) — captured here (not in child PR body)
- A worktree `PostToolUse` rustfmt hook rewrote a one-line `if … { continue; }` into a
  3-line block between two Edits, breaking a later `old_string` match. Re-reading the
  reformatted region before the next Edit resolved it — expect this whenever a rustfmt
  hook is active in a worktree. (Overlaps existing worktree-friction kaizen #540/#487.)

### Orchestrator RETRO
- One-wave / one-issue sprint: of 67 open issues only #234 was schedulable. The backlog
  is dominated by a large `needs-decision` regex-dialect/frozen inbox and `kaizen` infra
  items that are (correctly) not `good-autonomous`. Not a defect — just noting the
  schedulable surface is nearly drained; most remaining value needs architect decisions.
- Issue #234 carried a stale body dependency ("Blocked on C7 (#232)") while #232 was
  already CLOSED/completed — had to verify #232's actual state rather than trust the body
  prose (matches the §3 "classify by label/comment state, not stale body" warning).
