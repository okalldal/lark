# Sprint 20260624-0fd2c76 — residue ledger (ADR-0023)

**Sprint base (master SHA):** `0fd2c76e8b25c52ea8c7930c80a4992080a848ec`
**Integration branch:** `sprint/20260624-0fd2c76`

This is the append-only irreducible-residue file: orchestrator/review-sub-agent
`RETRO:` notes and any synced-`master` SHAs. The authoritative durable record is
GitHub itself (kept branch merge history + child PRs + labels). The omnibus PR body
is a convenience summary, reconstructed at finalize — not maintained per stage.

## Schedulable plan (from §3, base `0fd2c76`)
- Wave 1 (disjoint): #417 standalone, #402 tree_builder/tree, #399 terminal/lexer
- Wave 2 (disjoint loader files): #414 loader compiler/tokenizer, #404 loader ebnf compile_expansion, #372 loader imports
- Wave 3: #401 lalr + ebnf inner_alternatives, #385 test-only chore (stale XFAIL flip)

## Synced master SHAs
- (none yet)

## RETRO (orchestrator + review sub-agents + notable worker notes)
- [worker #417, kaizen/infra] GitHub MCP token expired mid-task; `gh` CLI not installed in the worktree, so the worker could not claim the issue *before* coding (had to claim post-push). Worktree lacks `gh` as a fallback. Suggest: tracking-issue for transient MCP-token expiry + worktree `gh` availability.
- [worker #417, info] Issue Files list named only `runtime.rs`+`mod.rs` XFAIL, but adding the enum variant broke 3 other exhaustive `match` sites (`include_lark!` proc-macro test, `rt_matches` helper) + 2 regenerated fixtures — all within the standalone surface. Triage could note "adding an enum variant has a wider blast radius than the fix site."
- [worker #399, kaizen/infra] Same MCP-token-expiry-at-claim-step as #417 (recovered on retry); reinforces the worktree-`gh`-fallback kaizen.
- [worker #399, kaizen] Bash cwd resets between calls and does not persist `cd`; worker initially ran `cargo test` in the *shared* checkout instead of its worktree and saw stale code (XFAIL still ignored). Worker brief should warn: always prefix the worktree absolute path on every cargo/git invocation. (Related to #326.)
- [worker #399, info] Issue's fix contract ("retain pre-normalization source on `PatternRe`") under-specified: verbatim source is lost *upstream* of `PatternRe::new` (terminal-composition memo de-escapes leaves), so the real fix needed a loader-side override in `grammar/loader/terminals.rs` — beyond the named blast radius. ADR-0031 fix-site verification checked the *consumer* (`raw_value_len`) but not that its input was already normalized. Reviewer judged the widening in-scope (serves the same done-when).

- [worker #402, kaizen/infra] Same MCP-token-expiry-at-claim-step (recovered later); reinforces worktree-`gh`-fallback kaizen.
- [worker #402, info] Files list (`tree_builder.rs`+`tree.rs`) under-specified: engine-agnostic fix required plumbing `propagate_positions` (a no-op flag until now) through `CompiledGrammar`/`ParseTable`/`parsers/mod.rs` + a non-trivial Earley resolve-streaming container stack — broader than named blast radius (+2460/-25, 11 files).
- [worker #402, validates-process] Earley's resolve path filters tokens *before* `shape`, so the LALR/CYK assemble-based fix didn't cover it — needed a parallel-stack + rollback. **Banks-green alone would have hidden the Earley divergence; the targeted multi-engine meta differential caught it.** Concrete evidence the §5 differential-audit checkpoint earns its keep.
- [review #426, tooling] Large PR diff (+2460) overflowed the MCP token cap on BOTH `get_diff` AND `get_files` (even the file-list form); reviewer had to delegate the diff read in chunks. Expect to delegate diff reads for any large sprint child.
- [follow-up filed by worker #402] #425 — standalone parsers ignore `propagate_positions`/meta (pre-existing gap, not a regression). Product follow-up.

- [worker #414, kaizen] First Read/Edit hit the *shared* checkout path and the Edit tool rejected with a worktree-isolation error; had to re-Read under the worktree absolute path. Front-load the worktree path on the very first Read. (Recurs across workers → strong kaizen, relates to #326.)
- [worker #414, validates-process] `/code-review` caught a genuine too-permissive bug (backslash-before-newline skipped the newline byte) the first cut missed — foreground review was load-bearing.
- [worker #404, kaizen] Bash cwd reset confirmed real (brief warned); also a deterministic perf counter read *after* the workload still hangs if the workload itself is the blowup — cap k so the gate fails fast. General lesson for build-time perf gates.
- [worker #372, kaizen/infra — ENOSPC] Disk hit 100% mid-gate (concurrent worktree `target/` dirs ~25G + shared checkout 5.2G). Freed via `cargo clean` on the *shared* checkout's stale target only. **Directly reproduces kaizen #313** (recurring shared-disk ENOSPC from concurrent worktrees) — confirm #313 captures it; sprint infra may want a shared-target or periodic-clean policy.
- [worker #372, kaizen] `cargo test --test A --test B <name>` filters the name across ALL listed binaries; a probe whose name doesn't match shows "filtered out" rather than running — run probes in their own `--test` invocation.

## Follow-ups filed by workers (product)
- #425 — standalone parsers ignore `propagate_positions`/meta (from #402; pre-existing, not a regression).
- #428 — RC1-class: a user rule colliding with a mangled interior import origin is silently merged; reproduces with a single import (from #372; independent root cause).

## Post-finalize (architect review of omnibus #422)
- Architect verdict (chat, 2026-06-24): **no blocking findings; would merge #422 after branch-protection checks.** One non-blocking ask: keep visible that #399/#424 restores verbatim raw-length only for a single-`/regex/`-literal body; composite bodies keep `raw == pattern` (narrower than the `raw_value_len()==len(pattern.value)` contract).
- **Filed #437** (`needs-decision`) for the composite-regex-body raw-length gap (extend verbatim capture to composites vs ADR-0017 diverge-&-document). Added to the product follow-up arm.

## Staged
- #423 (issue #417, tier auto) → squash 10bf2bc
- #424 (issue #399, tier auto) → squash 8e0b20b
- #426 (issue #402, tier auto) → squash 393caa7
- #427 (issue #414, tier auto) → squash 845cb1d
- #429 (issue #404, tier auto) → squash f12db3d
- #430 (issue #372, tier auto) → squash 0fed372
- #431 (issue #385, tier auto) → squash b1d6e32
- #433 (issue #401, tier auto) → squash e54fdf8

## Wave 3 residue
- [follow-up filed by worker #401] #432 — H6-3's secondary Earley `al1`-vs-`al2` resolution divergence (distinct root: SPPF `add_family` dedup in earley.rs; banks-gated). Product follow-up. Reviewer confirmed legit split, NOT an in-scope punt (the two un-ignored XFAILs are LALR-only).
- [worker #401, kaizen] Test file `test_bounty_findings_h6.rs` was silently reverted to origin mid-session (after edits + a passing run); src edits survived; had to re-apply. Cause unclear (external checkout/reset?). Verify `git status` against the worktree absolute path before pushing.
- [review #433, kaizen — issue authoring] #401's "Expected fix contract: support-and-match" line was looser than its crisp "un-ignore both XFAILs" done-when (one implies Earley in-scope, the other doesn't); forced the reviewer to adjudicate scope. Keep the "Expected fix contract" consistent with the falsifiable done-when. One-line note for issue-authoring guidance.

## RETRO — ENOSPC mid-sprint (orchestrator + #385 review, kaizen — reinforces #313)
- [orchestrator + review #431, kaizen/infra] Mid-sprint the shared volume hit **100% (896K free)** from 8 concurrent `isolation: worktree` checkouts each carrying a 4–5 GB `target/` (~30G total). This (a) failed the orchestrator's own pre-push `cargo test --all` hook on a *doc-only* residue commit, and (b) forced worker #385 to push with `--no-verify` because its local gate could not even link (`No space left on device` / `ld ... Bus error`). NOT a code regression — once disk was freed the integrated branch passed the full local gate, and every child was green on CI. **Orchestrator mitigation:** `git worktree remove --force` on the 6 completed+merged workers' worktrees (kept the 2 running) → freed ~27G. **Kaizen (reinforces #313):** sprint concurrency needs either a shared cargo `target/` (CARGO_TARGET_DIR) across worktrees, a per-child `target/` cleanup step, or a lower worktree concurrency cap; otherwise workers starve each other's local gates and reliance silently shifts to CI. Confirm #313 covers this and add the worktree-cleanup mitigation as a documented step.
