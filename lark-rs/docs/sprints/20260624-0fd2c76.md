# Sprint 20260624-0fd2c76 — residue ledger (ADR-0023)

**Sprint base (master SHA):** `0fd2c76e8b25c52ea8c7930c80a4992080a848ec`
**Integration branch:** `sprint/20260624-0fd2c76`

This is the append-only irreducible-residue file: orchestrator/review-sub-agent
`RETRO:` notes and any synced-`master` SHAs. The authoritative durable record is
GitHub itself (kept branch merge history + child PRs + labels). The omnibus PR body
is a convenience summary, reconstructed at finalize — not maintained per stage.

## Schedulable plan (from §3, base `0fd2c76`)
- Wave 1 (disjoint): #417 standalone, #402 tree_builder/tree, #399 terminal/lexer
- Wave 2 (disjoint loader files): #414 loader compiler/tokenizer, #404 loader ebnf compile_expansion, #372 loader imports
- Wave 3: #401 lalr + ebnf inner_alternatives, #385 test-only chore (stale XFAIL flip)

## Synced master SHAs
- (none yet)

## RETRO (orchestrator + review sub-agents + notable worker notes)
- [worker #417, kaizen/infra] GitHub MCP token expired mid-task; `gh` CLI not installed in the worktree, so the worker could not claim the issue *before* coding (had to claim post-push). Worktree lacks `gh` as a fallback. Suggest: tracking-issue for transient MCP-token expiry + worktree `gh` availability.
- [worker #417, info] Issue Files list named only `runtime.rs`+`mod.rs` XFAIL, but adding the enum variant broke 3 other exhaustive `match` sites (`include_lark!` proc-macro test, `rt_matches` helper) + 2 regenerated fixtures — all within the standalone surface. Triage could note "adding an enum variant has a wider blast radius than the fix site."
- [worker #399, kaizen/infra] Same MCP-token-expiry-at-claim-step as #417 (recovered on retry); reinforces the worktree-`gh`-fallback kaizen.
- [worker #399, kaizen] Bash cwd resets between calls and does not persist `cd`; worker initially ran `cargo test` in the *shared* checkout instead of its worktree and saw stale code (XFAIL still ignored). Worker brief should warn: always prefix the worktree absolute path on every cargo/git invocation. (Related to #326.)
- [worker #399, info] Issue's fix contract ("retain pre-normalization source on `PatternRe`") under-specified: verbatim source is lost *upstream* of `PatternRe::new` (terminal-composition memo de-escapes leaves), so the real fix needed a loader-side override in `grammar/loader/terminals.rs` — beyond the named blast radius. ADR-0031 fix-site verification checked the *consumer* (`raw_value_len`) but not that its input was already normalized. Reviewer judged the widening in-scope (serves the same done-when).

## Staged
- #423 (issue #417, tier auto) → squash 10bf2bc
