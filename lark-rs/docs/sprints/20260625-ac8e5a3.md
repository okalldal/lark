# Sprint 20260625-ac8e5a3 — residue ledger (ADR-0023)

**Sprint base (master SHA):** `ac8e5a3c61bd4992d612d02bd2e5576b2059debb`
**Integration branch:** `sprint/20260625-ac8e5a3`

This is the append-only irreducible-residue file: orchestrator/review-sub-agent
`RETRO:` notes and any synced-`master` SHAs. The authoritative durable record is
GitHub itself (kept branch merge history + child PRs + labels). The omnibus PR body
is a convenience summary, reconstructed at finalize — not maintained per stage.

## Schedulable plan (from §3, base `ac8e5a3`)

13 `good-autonomous` issues, grouped into waves by blast-radius (same-file ⇒ serialize
across waves; cap ~3 concurrent). Fix-site files in brackets.

- **Wave 1** (disjoint): #403 [loader/terminals.rs `patterns_equivalent`], #432 [parsers/earley.rs `add_family`], #428 [loader/compiler.rs+imports.rs RC1 dup-rule]
- **Wave 2** (disjoint): #286 [loader/terminals.rs imported-terminal extend], #400 [grammar/terminal.rs `normalize_python_escapes`], #362 [loader/compiler.rs `dedup_and_check_alts`]
- **Wave 3** (disjoint): #366 [loader/terminals.rs `TERMINAL_NAMES`], #360 [grammar/terminal.rs `Pattern::max_width`], #361 [loader/tokenizer.rs name-shape]
- **Wave 4** (disjoint): #364 [grammar/terminal.rs regex dialect screening], #405 [loader/tokenizer.rs letterless names], #337 [src/tree.rs `Meta::from_children`]
- **Wave 5**: #425 [src/standalone/ propagate_positions/meta]

Serial chains (same file, run across waves so staging serializes them):
- loader/terminals.rs: #403 → #286 → #366
- grammar/terminal.rs: #400 → #360 → #364
- loader/tokenizer.rs: #361 → #405
- loader/compiler.rs: #428 → #362
- independent files: #432 (earley.rs), #337 (tree.rs), #425 (standalone/)

## Synced master SHAs
- (none yet)

## Verdicts (review sub-agent tier rulings — orchestrator-recorded, ADR-0023)
- #440 (issue #403): **DoD PASS, tier `auto`**. Differential audit satisfied (keep_all_tokens typing probe proves the merge; `/[ab]/` control proves no over-correction). CI green confirmed → **staged** (squash 6ea1780).
- #441 (issue #432): **substantively PASS, tier `auto`** (review FAIL was solely "CI test pending"; CI then went green). Reviewer scrutinized the SPPF dedup hard: keep-ONE-lowest-order (not the issue's keep-both sketch, which spurious-`_ambig`s under explicit), explicit-mode Python-faithful, non-ε `(left,right)` dedup undisturbed, #159 guard tests intact, ε-family index can't be invalidated. CI green → **staged** (squash 7bb8201).
- #443 (issue #428): **DoD PASS, tier `auto`**. Reviewer validated the differential audit: rejects in BOTH document orders, #372 import-vs-import dedup NOT regressed, no false-reject of a dropped last-alias-wins alias (worker's own-review bug fixed by keying on narrow `claimed_rule_names`/`alias_survives`, not over-inclusive `reserved_rule_names`), guard gates on `mangled==true` (spares #343). Out-of-scope find #442 is a legitimate separate-scope deferral. CI green → **staged** (squash b2f1784). (Recovered from a stranded worker — see RETRO.)
- #446 (issue #361): **DoD PASS, tier `auto`**. `__`-leading name rejection across all 4 surfaces (rule/terminal def, alias target, template param); boundary (`_x`/`x__`) preserved; composes with #405 (#405 XFAIL still fails). Necessary consequences in-scope: synthetic probe rules renamed (`__lark_import_probe`→`_lark_import_probe`, `__common_probe`→`_common_probe`, else re-lex self-rejects), #144 tests rewritten to assert rejection (id-space protection now enforced one layer earlier at the tokenizer). **Net diff vs current tip independently verified #361-only — #428 `claimed_rule_names` untouched, RC1 tests 34/0 intact** (PR's get_files diff is merge-base-relative → showed #428 as spurious additions). CI green → **staged** (squash ccc5b35).
- #451 (issue #400): **DoD PASS, tier `auto`**. `normalize_python_escapes` rewrites `{,n}`→`{0,n}` — class-aware (skips `[...]`) + escape-aware (skips `\{`), only `base_quantifier_len`-valid n≥1. Failed-first XFAIL passes (`/a{,3}b/` matches `aaab`, rejects `aaaab`); inverted-bound `/a{3,2}b/` negative control still rejects on all 4 engine configs. Net diff verified 2-file (terminal.rs + test_h6) — no #428 contamination (worker rebased onto ccc5b35). Out-of-scope finds #447/#448 legit deferrals. CI green → **staged** (squash 46bb3c9).
- #450 (issue #286): **DoD PASS, tier `auto`**. Imported-terminal `%extend` stages new alts in `pending_term_extends`, prepended in `resolve_terminals` (width/leftmost-first ranked). Failed-first XFAIL passes (`%extend INT: "z"` → both `"z"` and `"123"` parse); self-recursion rejected; same-grammar extend / imported `%override` / abstract-`%declare` reject all NOT regressed; LALR 512/512. Net diff verified 3-file (terminals.rs/compiler.rs/test_h2) — #428 `claimed_rule_names`/RC1 untouched (0 occurrences). Out-of-scope find #449 legit deferral. Minor parity nuance (arm sort omits `-min_width`, consistent with existing `lexer/plan.rs` convention) folded into #449. CI green → **staged** (squash f97cf42). [Wave 2 complete]
- #452 (issue #366): **DoD PASS, tier `auto`**. Added exactly the 2 missing `TERMINAL_NAMES` rows (`"\\"`→BACKSLASH, `"\r\n"`→CRLF), matching Python's `_TERMINAL_NAMES`; non-shadowing (unique keys, distinct from the 35 single-char rows). Failed-first XFAIL passes both surfaces; oracle regen zero drift (no fixture used these names). Net diff verified 3-file (terminals.rs/test_h5/BOUNTY doc); composes with #286. CI green → **staged** (squash a4cf0c7).
- #453 (issue #405): **DoD PASS, tier `auto`**. `reject_letterless_name` (rejects names with no ASCII letter) added to lex_rule/lex_terminal, COMPOSES with #361's `reject_double_underscore_name` (both present, verified — `__foo` still rejects via #361). All letterless forms (`_`/`__`/`_9` + terminal analog) reject; boundary `_x`/`x__` preserved; no over-reach on all-caps terminals. Net diff verified 2-file (tokenizer.rs/test_h6). CI green → **staged** (squash 3275749).
- #456 (issue #360): **DoD PASS, tier `auto`**. `Pattern::max_width` Err-arm now sizes `regex_syntax`-rejected lookaround terminals via the shared assertion-aware width walk (`lookaround::pattern_max_width`, assertions zero-width) instead of None→unbounded; parseable HIR path (RC5/#268) provably untouched; sort key unchanged. Failed-first XFAIL passes (REG mw2 beats LA mw1, both lexers + lookbehind). Net diff verified 3-file (terminal.rs/lookaround/mod.rs/test_h5) — no #400 `normalize_python_escapes` touch, no leaked probe. Out-of-scope finds #454/#455 legit deferrals. CI green → **staged** (squash 10b8b7e). [Wave 3 complete: 9/13]
- #458 (issue #362): **DoD PASS, tier `auto`** — **TEST-ONLY** (0 src lines). The #362 reduce/reduce bug was ALREADY FIXED on the sprint branch by #347/#378's `dedup_and_check_alts` empty-arm canonicalization (`if syms.is_empty() { Vec::new() }`) — the XFAIL passed on intake. Per oracle-first/ADR-0031 the worker correctly did NOT fabricate a redundant src change; instead promoted the XFAIL to a permanent guard + hardened it (both MP modes, order-flip first-empty-wins, multi-symbol, 2-bracket, controls) + added `h5_3_empty_arm_collapse_does_not_over_collapse` (non-empty colliding arms still reject — over-collapse tripwire). Reviewer confirmed src already implements the fix. CI green → **staged** (squash 47f4854).
- #459 (issue #425): **DoD PASS, tier `escalate`** ⚠️ — option (b) fail-loud: standalone `bake()` now rejects `propagate_positions=true` with a typed `GrammarError` at the shared chokepoint covering ALL 3 entry points (CLI / `include_lark!` / `generate_standalone`), closing the silent ADR-0017 asymmetry per ADR-0030. Filed #457 (option a, full standalone Tree.meta parity). Failed-first test (in-process ACCEPTS the config → standalone now ERRORS), default builds unaffected. **Tier escalate (NOT auto): introduces a NEW user-visible rejection on the public standalone surface — ARCHITECT MUST RATIFY via the omnibus.** Net diff 2-file standalone-scoped. CI green → **staged** (squash e901ea0).
- #463 (issue #364): **DoD PASS, tier `auto`** — H5-6: `(?<name>…)` angle named-group now rejects as `InvalidRegex` (Python has only `(?P<name>)`), with the critical differential preserved (`(?P<`, lookbehind `(?<=`/`(?<!`, in-class `[(?<x>]`, escaped `\(?<x>` all still build); H5-5: re-bucketed `\N{NAME}` from the mislabelled `LookaroundScope` → `InvalidRegex` (the issue's sanctioned FLOOR). Full `\N{NAME}` support (needs a 138k-entry Unicode-name table) deferred to #461; the supported-XFAIL stays honestly `#[ignore]`d re-pointed to #461 (not silently flipped). Single `PatternRe::new` seam (no bypass). Net diff 2-file, composes with #360 (max_width untouched). CI green → **staged** (squash 2ec37b6). [Wave 5 complete: **13/13** staged]
- #460 (issue #337): **DoD PASS, tier `auto`** — `Meta::from_children` sets `empty = meta.line.is_none()` (true iff no child contributed a position) instead of only `children.is_empty()`, matching Python's `Meta.empty`. Failed-first XFAIL passes (`start.meta.empty==true` + inner subtree); position-bearing control unaffected; span fields untouched; `propagate_positions` oracle bank pins both directions. Net diff verified #337-only (worker hit a stale-ref revert of #452/#456 mid-task, caught via pre-push `git diff --stat` + recovered — final diff clean). CI green → **staged** (squash 4a9cf11). [Wave 4 complete: 12/13]

## RETRO (orchestrator + review sub-agents + notable worker notes)
- [review #440, awareness] The shared/local checkout sits on the sprint base, not the PR head, so a reviewer reading `terminals.rs` on disk sees the pre-fix code — the PR *diff* is the source of truth. No action; just front-load "read the diff, not the working tree" for verdict-only reviewers.
- [review #440, validates-§6] CI `test` (full gate) was mid-flight at review time; verdict-only reviewer can confirm evidenced banks-green from the PR body but not the final gate — the orchestrator's green-poll before staging is load-bearing (consistent with §6 "poll for green explicitly").
- [worker #403, candidate-followup] `impl PartialEq for Pattern` (`grammar/loader/terminal.rs`) is NOT kind-gated and would treat `PatternStr("ab") == PatternRe(/ab/)` as equal — but it is currently *unreached* (no `==`/`Hash`/`HashMap` path compares `Pattern` across kinds), banks green. Worker correctly deferred as optional architect-directed tidy-up (in PR #440 body). → consider a low-prio product follow-up at close-out, not a DoD failure.
- [review #441, tooling — KAIZEN] `mcp__github__pull_request_read get_status` returns `total_count:0` for this repo (combined-status API empty); the reliable CI source is `get_check_runs`. Reviewers/orchestrator must use `get_check_runs`, never `get_status`, to judge CI here. (Reinforce in review brief + §6.)
- [review #441, awareness] Verdict-only reviewers' local worktree is parked on the sprint tip, NOT the PR head — grepping the worktree reflects the baseline, not the PR. Read correctness off the diff. (Recurs across reviews; reinforce in the §5 review brief.)
- [orchestrator, KAIZEN — reinforces #392/#309] Worker #428 STRANDED: it spawned **background** review sub-agents, idled awaiting them, and hit its ~200k token cap with the fix **uncommitted** (no branch pushed, no PR) + a stray untracked `examples/probe_428.rs` left in its worktree. The brief explicitly forbade background reviewers and required atomic review→push→PR→return — yet the worker still did it (same failure as sprint #284). Recovered by `SendMessage`-resuming the worker with a tight foreground-only finish directive. Kaizen #392 already tracks "sprint review sub-agents must be mechanically read-only + foreground" — confirm it also covers the *worker's own pre-PR review* (the worker, not just the sprint reviewer, must run its review foreground), and consider a mechanical Stop-hook guard for unpushed worktree work (#324).
- [orchestrator + worker #361, KAIZEN — reinforces #326] **Stale-worktree contamination.** Worktrees created at dispatch (`ce91368`, before #428 staged) lag the advanced sprint tip; after the worker's `git checkout origin/sprint`, the worktree HEAD advanced but a stale uncommitted REVERT of the later-staged #428 files (`imports.rs`/`compiler.rs`/`test_bounty_findings.rs`) appeared in the working tree. Worker #361 caught it only via `/code-review`'s cross-file angle and restored to sprint base; an `add -A`/`commit -am` worker would have silently shipped a #428 revert in its PR and regressed a staged fix on staging. Mitigations now in force: (a) orchestrator NET-DIFF-VERIFIES every child vs the *current* sprint tip (`git diff origin/sprint..pull/N/head`) before staging — the PR's own get_files is merge-base-relative and misleading; (b) protective heads-ups sent to in-flight #286/#400. **Kaizen:** worker step-0 should `git reset --hard origin/sprint` (or `git checkout -f`) after the branch checkout to force the working tree to the tip, and workers should `git add` only their intended files (never `-A`/`-am`). File as a kit fix (reinforces #326/#324).
- [review #446, tooling] PR `get_files`/`get_diff` are merge-base-relative; on a moving integration branch they show already-staged sibling changes as spurious additions. Verdict-only reviewers must read the NET diff (`git diff origin/sprint..pull/N/head`). (Reinforces #435 — large/misleading sprint-child diffs; reviewers must compute the true delta.)
- [review #446, KAIZEN — reinforces #313] Local `check-fast.sh` hit `No space left on device` (disk quota-capped ~1G free) mid-gate; worked around with `cargo clean` + `CARGO_INCREMENTAL=0` + per-component runs. Recurring concurrent-worktree-`target/` ENOSPC — #313 territory; a shared `CARGO_TARGET_DIR` or per-child cleanup is the durable fix.
- [orchestrator, minor] Don't `git fetch origin pull/N/head:localbranch` in the SHARED checkout while staging — it left the shared checkout on the fetched branch, so a later `git pull --ff-only` aborted (non-ff from the worker's pre-squash commit). Net-diff verification belongs in a throwaway worktree, or re-checkout the sprint branch immediately after the fetch. (No work lost — residue was all pushed.)
- [orchestrator, KAIZEN — root cause of the branch thrash] **Background verdict-only review sub-agents (no `isolation: worktree`) run in the SHARED checkout and were running `git fetch …:prN` / checkout there**, mutating the orchestrator's working branch under it (the stray `pr451` branch + the `pr446` HEAD switch both came from this). Fix adopted mid-sprint: (a) orchestrator net-diff-verifies via non-checkout `refs/prcheck/N` refs (`git diff $(git merge-base origin/sprint refs/prcheck/N)..refs/prcheck/N`) — never changes HEAD; (b) review sub-agents are now briefed **GitHub-API-only, no local git/cargo**, handed the orchestrator-verified net-diff scope. File as kit fix: either give sprint reviewers `isolation: worktree` or mandate the API-only brief.
- [orchestrator, KAIZEN — reinforces #313, confirmed real] Mid-sprint the shared disk hit **100% (35M free)** from 6 concurrent worker-worktree `target/` dirs (~29G). It failed the orchestrator's own pre-push `cargo test --all` hook on a **doc-only** residue commit (a spurious "cargo test --all failed" that the diff cannot have caused — NOT a code regression; the omnibus CI on the same integrated tip was green). Mitigation: `git worktree remove --force` on all 6 completed workers' worktrees → freed to 30G, push then passed. Durable fix needed: shared `CARGO_TARGET_DIR` across worktrees, per-child cleanup, or lower concurrency. The "doc-only push pays full Rust gate" cost (#325) makes this worse.
- [review #450, parity-nuance] Imported-terminal `%extend` arm sort uses `(-max_width, -raw_value_len)`, omitting Python's `-min_width` term; consistent with the pre-existing `lexer/plan.rs::sort_terminals` convention, unobservable across the 5 evidenced cases, folded into #449. Confirm-or-pin only if a min_width-discriminated ordering ever surfaces.
- [orchestrator, KAIZEN — 3rd stranding, STRENGTHENS #392] Workers #405 AND #360 BOTH stranded the same way as #428: the `/code-review` skill / Explore reviewer sub-agents default to `run_in_background: true`, share the worker's `isolation: worktree` filesystem, and one even WROTE a probe file into the worktree source (corrupting #360's uncommitted diff — a `// TEMP-ANALYSIS-ONLY` stub replaced a real docstring + a stray `zzz_temp_width_probe` in lower.rs). #405 I recovered via resume; #360 self-recovered via a `git status --porcelain` pre-`git add` gate. THREE of nine workers hit this despite explicit briefs → the brief is not enough; this needs a MECHANICAL fix (the `/code-review` skill itself must run finders foreground in a sprint-worker context, or workers need a hard pre-PR gate). High-priority kaizen; file at close-out (strengthens #392, relates to #324 Stop-hook).
- [review #360/#456, note] The shared lookaround `width_range` walk is now load-bearing on EVERY LALR/basic/contextual build (not just dynamic lexer), widening #455's (deep-nest recursion stack-overflow) blast radius — worth re-prioritizing #455 above its `prio:later` if an adversarial-grammar-hardening epic surfaces. #456's `pattern_max_width` returns `Option<Option<usize>>` (outer=parseability, inner=width); the `.flatten()` call site is correct but subtle — keep the `unparseable_pattern_reports_outer_none` test verbatim on any refactor.
- [orchestrator, validates-process] The orchestrator-owned NET-DIFF-VERIFY gate (`git diff $(git merge-base origin/sprint refs/prcheck/N)..refs/prcheck/N`, non-checkout) caught/cleared every contamination risk before staging across all of Wave 2+3+4 (#446 #361-only despite merge-base-inflated get_files; #450/#453/#456/#458/#459/#460 all confirmed no sibling-fix regression). NO staged fix was regressed. This gate + the GitHub-API-only reviewer brief are the two mitigations that held the line; both should be baked into the kit.
- [orchestrator + worker #337, KAIZEN — FLAW in the reset --hard step-0 fix] My Wave 3+ "Worktree step 0: `git reset --hard origin/sprint`" fix has its OWN failure mode: `git fetch origin sprint/<b>` (no explicit refspec) can leave the worktree's `origin/sprint/<b>` REMOTE-TRACKING ref STALE, so `reset --hard origin/sprint` resets to the stale ref and REVERTS recently-staged siblings (#337's worktree reverted #452/#456; the orchestrator's OWN shared checkout was also one stage stale — both force-updated). #337 caught it via the pre-push `git diff origin/sprint --stat` check. **Durable fix:** workers (and orchestrator) must fetch with an explicit refspec `git fetch origin sprint/<b>:refs/remotes/origin/sprint/<b> --force` OR `git rev-parse origin/sprint/<b>` and assert it equals the packet's tip SHA BEFORE `reset --hard`; and always `git diff origin/sprint --stat` right after reset (not just at push). The packet's "current tip SHA" is an assertion to verify, not a given. File as kit fix.
- [worker #362, validates-process] ADR-0031 "confirm the hypothesised fix site against the failing XFAIL FIRST" paid off concretely: #362's bug was already fixed by a sibling PR's side effect (#347/#378), so the right deliverable was XFAIL-burndown hardening, NOT a fabricated src change. Keep that instruction prominent for `good-autonomous` bug issues whose fix sites are marked "hypothesised".
- [orchestrator, KAIZEN — ENOSPC forced --no-verify TWICE] Workers #362 and #425 BOTH hit shared-disk 100% during their pre-push fast gate and pushed `--no-verify` (CI is authoritative; both green). The `lark_proc compile_fail` trybuild test is especially disk-hungry (builds a throwaway dep-tree under `target/tmp/`). Orchestrator mitigated by `git worktree remove --force` on each completed worker (kept disk 30–50% in the back half). Strongly reinforces #313/#325 — a shared `CARGO_TARGET_DIR`, per-worker `target/` quota, or a lower concurrency cap is the durable fix; the kit should also document "remove a worker's worktree as soon as its PR is staged" as standing orchestrator hygiene.

## Follow-ups filed (product)
- #442 — `%override`/`%extend` of an interior import origin: pre-existing reject-where-Python-accepts divergence (filed by worker #428 while fixing the RC1 user-vs-import collision).
- #445 — `_9x` third name-shape class (1 underscore → digit → letter) Python rejects; outside both #361 (`>=2`-underscore) and #405 (letterless) predicates (filed by worker #361).
- #447 — `{,}` is Python's `{0,}` == `*` (distinct from #400's `{,n}`); needs `base_quantifier_len` widened (filed by worker #400; corrected an earlier wrong "Python reads `{,}` as literal" claim).
- #448 — leading / "nothing to repeat" quantifier (`/*a/`, `/{0,3}/`) mis-categorized as OutOfScope lookaround; pre-existing and general (filed by worker #400).
- #449 — within-terminal arm sort in `resolve_term_regex` is string-length-based (pre-existing, independent of `%extend`) + residual where a new extend arm whose width falls strictly between two arms of a multi-alt imported body isn't interleaved (imported body kept as one unit) (filed by worker #286).
- #454 — lookaround width walk over-counts multi-char escapes vs `sre_parse` (pre-existing limitation of the shared walk, newly exercised by #360; cannot flip finite↔unbounded so below the ordering-accuracy bar) (filed by worker #360).
- #455 — lookaround front-end recursion can stack-overflow on deeply-nested terminal regex (pre-existing; #360 widened its blast radius to every LALR build — consider re-prioritizing) (filed by worker #360).
- #457 — full standalone `Tree.meta` + `propagate_positions` parity (option (a) for #425; lifts the #459 fail-loud rejection once landed) (filed by worker #425).
- #461 — full `\N{NAME}` named-unicode-escape support (vendor a Unicode-name→codepoint table; un-ignore `h5_5_named_unicode_escape_supported`); the option-(a) follow-up for #364's H5-5 re-bucket floor (filed by worker #364).
- #462 — incidental `{x}` literal brace-run mislabel found while fixing #364 (independent on the sprint tip) (filed by worker #364).

## Follow-ups filed (kaizen)
- #444 — stale CLAUDE.md #144 notes + keep/simplify the `reserved_rule_names` skip-forward (now defense-in-depth since #361); governance/docs, rides its own PR per §9 (filed by worker #361).
- TO FILE AT CLOSE-OUT (orchestrator/review RETRO above, confirm coverage / file if not tracked): worker-stranding reinforcement of #392/#309/#324 (worker's own pre-PR review must be foreground; Stop-hook for unpushed worktree work); stale-worktree contamination kaizen (worker step-0 `git reset --hard origin/sprint`; `git add` only intended files — reinforces #326); the `get_status`→`get_check_runs` reviewer note; merge-base-relative PR diff (reviewers read net diff — reinforces #435); recurring concurrent-worktree `target/` ENOSPC (reinforces #313).

## Staged (running aid — authoritative table reconstructed at finalize from merge history)
- #440 (issue #403, tier auto) → squash 6ea1780
- #441 (issue #432, tier auto) → squash 7bb8201
- #443 (issue #428, tier auto) → squash b2f1784   [Wave 1 complete]
- #446 (issue #361, tier auto) → squash ccc5b35
- #451 (issue #400, tier auto) → squash 46bb3c9
- #450 (issue #286, tier auto) → squash f97cf42   [Wave 2 complete: 6/13 staged]
- #452 (issue #366, tier auto) → squash a4cf0c7
- #453 (issue #405, tier auto) → squash 3275749
- #456 (issue #360, tier auto) → squash 10b8b7e   [Wave 3 complete: 9/13 staged]
- #458 (issue #362, tier auto, test-only) → squash 47f4854
- #459 (issue #425, tier **escalate**) → squash e901ea0   ⚠️ architect ratifies via omnibus
- #460 (issue #337, tier auto) → squash 4a9cf11   [Wave 4 complete: 12/13 staged]
- #463 (issue #364, tier auto) → squash 2ec37b6   [**13/13 staged — sprint complete**]

## Finalize (§7, ADR-0023)
- **All 13 schedulable `good-autonomous` issues staged.** Master `ac8e5a3` confirmed an ANCESTOR of the sprint tip (omnibus diff = on top of today's master). Staging table reconstructed from the branch's 13 squash-merges (each names `…(#PR)`), cross-checked against labels + this residue.
- **Tiers:** 12 `auto`, 1 `escalate` (#459/#425 — new public-standalone reject; **architect must ratify via omnibus**).
- **Issue→PR→squash:** #403→#440→6ea1780 · #432→#441→7bb8201 · #428→#443→b2f1784 · #361→#446→ccc5b35 · #400→#451→46bb3c9 · #286→#450→f97cf42 · #366→#452→a4cf0c7 · #405→#453→3275749 · #360→#456→10b8b7e · #362→#458→47f4854 · #425→#459→e901ea0 · #337→#460→4a9cf11 · #364→#463→2ec37b6.
- **Product follow-ups filed (12):** #442 #445 #447 #448 #449 #454 #455 #457 #461 #462 (+ pre-sprint #437; + the #403 PartialEq-for-Pattern candidate noted, unfiled).
- **Kaizen (to file at §9 close-out, durably recorded above):** worker-own-review-backgrounds-and-strands (3×, extends #392); sprint-reviewer-git-isolation + reset-`--hard` stale-ref flaw (relates #326/#324); `get_status`-empty→`get_check_runs` + merge-base-relative-diff (relates #435); recurring concurrent-worktree ENOSPC + remove-worktree-when-staged (reinforces #313/#325).

## Dynamic schedule note (re-sequenced from §3 plan)
Wave 1 (#403,#432,#428) all staged. #362 (compiler.rs) was deferred out of the literal
"Wave 2" until #428 staged (now done → #362 schedulable). Pulled #361 (tokenizer.rs)
forward into the in-flight wave with #286 (terminals.rs, after #403) + #400 (grammar/terminal.rs).
Remaining ready/queued heads: #362 (compiler.rs, now unblocked), #366 (terminals.rs, after #286),
#360 (grammar/terminal.rs, after #400), #405 (tokenizer.rs, after #361), #364 (grammar/terminal.rs, after #360),
#337 (tree.rs, independent), #425 (standalone/, independent).
