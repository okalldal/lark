# Sprint residue ledger — 20260701-882c5b8

**Sprint base (master SHA):** `882c5b87eadb5203fb99ac0dd42684eaea0c440c` (`882c5b8`) —
the merge of the prior sprint's omnibus #584 (C9/#234). CI green at dispatch.
**Integration branch:** `claude/start-sprint-dfdt44`
**Omnibus PR:** #587 (draft until finalize)

This is the append-only irreducible-residue ledger (ADR-0023): orchestrator/review
`RETRO:` notes and any synced-`master` SHAs. Worker `RETRO:` persists in child PR bodies;
parked memos on issues; follow-ups as filed issues — not duplicated here. The *Staged*
table is reconstructed at finalize from the integration branch's merge history, not
maintained live.

## Scheduled issues (schedulable set)

- **#582** — Semantic output C8.1: span-emitting lexer path for `parse_span` (no
  `Token.value` allocation). `good-autonomous`, `prio:later`. Wave 1.
- **#583** — Semantic output C8.2: bounded child-buffer reuse counter + gate.
  `good-autonomous`, `prio:later`. Wave 2 (serialized after #582 — shared SpanTree/output
  blast radius).
- **#568** — Guard-body dense DFAs not budget-gated. `good-autonomous`, `prio:later`.
  Wave 1 (independent lexer `guard.rs`/`dfa.rs` subsystem).

## Context

Fresh sprint invoked immediately after the architect merged omnibus #584 (base
`ef5dcd3` → master `882c5b8`). These three `good-autonomous` follow-ups were filed/labeled
after #584's plan snapshot and were not in that pass; the architect greenlit picking them
up on the new base.

## Synced-master SHAs

- (none yet)

## RETRO (orchestrator / review)

- (append as produced)
