# Sprint 20260622-1100 — residue ledger

**Sprint base (`master`):** `a005423b0e94870d2492e0032ab2dd501a043656`
**Integration branch:** `claude/start-sprint-14o62b`
**Omnibus PR:** #255

This is the append-only **irreducible residue** for the sprint (ADR-0023): the
orchestrator's and review sub-agents' `RETRO:` notes, and any synced-`master` SHA.
It is **not** the source of truth for staging — that is reconstructed at finalize
from the integration branch's merge history + child PR bodies. Worker `RETRO:`
notes live in their child PR bodies; parked memos live on the issues.

## Schedulable plan (locked)

- Wave 1: #251 (lalr.rs+lib.rs), #252 (ebnf.rs), #253 (generate_oracles.py)
- Wave 2: #228 (parsers/ C3), #250 (generate_oracles.py+test_interactive.rs), #79 (benches/)
- Wave 3: #230 (perf.rs C5), #229 (C4 parity — unblocks after #228 stages)

## Not sprint-schedulable (close-out memo)

- #232 (C7 public API) — escalate-tier, not good-autonomous; architect call.
- #225 / #209 / #208 — epic umbrella trackers.
- #244 / #243 / #242 — needs-decision (architect inbox).
- #234 / #233 — blocked on C7 (#232).
- #165 / #164 — prio:later, beyond-oracle, deferred.

## Parked (needs-decision / blocked)

- **#253** — *parked `needs-decision`* (worker, wave 1). The exit-non-zero change is
  implementable and produces byte-identical fixtures, BUT the generator already emits
  **6 pre-existing contradictions** baked into committed oracles (in-repo `lark/` is
  1.3.1, which rejects `3.14j`, `.5j`, `0x_1A`, `0b_1010`, `0o_17`, and the `csv`
  `'#h1,h2\n…'` case that curated `should_pass=True` cases expect to parse). So
  exiting non-zero would redden the green "Oracle freshness" CI step despite identical
  fixtures (§2.4 regress-a-green-corpus). Fork (architect): (1) land as-is + a precursor
  PR fixing the 6 contradictions; (2) make the gate opt-in (`--strict`, off in CI); (3)
  fix the 6 contradictions first, then #253 lands trivially. Memo on issue #253;
  labels: removed `status:in-progress` + `good-autonomous`, added `needs-decision`.

## Already-implemented (recommend architect close — not in omnibus)

- **#79** — *already done* (worker, wave 2). The bench swap to the real bundled
  `python.lark` already landed via **#78** (commit `6fe8a07`) before this sprint. All four
  done-when conditions verified on sprint tip `7b056ae`: blockers #40/#97 closed;
  `python.lark` parses end-to-end under LALR+contextual+PythonIndenter (`file_input`);
  `benches/vs_python_lark.{rs,py}` both `include`/read the in-tree `python.lark`;
  `cargo bench --bench vs_python_lark` runs (real grammar over 124,605 bytes, byte-identical
  both engines, ~9.0x LALR / ~61.6x Earley). No child PR (no-op diff). Evidence comment on
  #79. **Interpretation point for the architect:** the done-when said "freeze its oracle";
  the bench has never used a committed oracle JSON — fairness is enforced in-process
  (both engines parse-check the byte-identical input before timing). If that satisfies
  "freeze the oracle", close #79 as completed referencing #78; otherwise a small follow-up
  to commit a frozen oracle.

## Synced-`master` SHAs

(none yet)

## RETRO (orchestrator + review sub-agents)

- **(worker #253, no child PR → recorded here)** Worktree gotcha: first Read hit the
  shared-checkout path; Edit refused until the worktree copy was re-Read. Do the
  worktree checkout (step 0) before any Read. — The brief's "must still exit 0 on
  current grammars" precondition was *falsified by the environment*: in-repo `lark` is
  1.3.1, not the version the curated cases were authored against; only running the
  generator reveals it. Future infra-gate issues should note the in-repo lark version
  as a hidden premise.
- **(worker #79, no child PR → recorded here)** Infra/swap-style issues whose deliverable
  can ride in on a sibling PR (here #78) should get a cheap pre-claim grep of the target
  files against the done-when before a full build/bench run — would have surfaced
  "already done" in seconds. Suggest `/triage` re-check such issues against current HEAD.
- **(orchestrator)** #253's done-when was not actually safe-to-land in the current
  environment — a triage-time `python3 tools/generate_oracles.py` run would have caught
  the 6 baked-in contradictions before labelling it `good-autonomous`. Suggest `/triage`
  run the generator for infra-gate issues that assert "exits 0 today".
- **(worker #252, not in PR #259 body → recorded here)** (a) The issue framed the `~n`
  blow-up as a lark-rs-only bug, but Python *itself* has the same `2^n` exponential and
  just rejects slowly — matching the verdict faster is a pure efficiency win, not a
  divergence; scope "blow-up" issues noting the oracle's own pathology. (b) The headline
  `[A]~2 C` was one of a family (`[A] [A]`, `[A]~0..1 C`, `([A])? C`, nested) across three
  code paths (`try_distribute`/`inline_repeat`/`present_forms`); a per-case oracle sweep
  caught two sibling paths + a pre-existing `mp=true` bug (#258) the headline test alone
  would miss. (c) The live oracle diff — not the test file — surfaced each intermediate
  regression. Lesson: differential-audit issues need the adversarial *family*, not just
  the named case.
- **(orchestrator)** **Worktree-isolation failure (kit bug):** the #79 worker switched the
  *shared* main checkout onto a new branch `work-79-python-lark-bench` instead of staying
  in its `isolation: worktree` checkout (its own worktree never appeared in `git worktree
  list`). My ledger commit landed on that stray branch; recovered by re-pointing
  `claude/start-sprint-14o62b` (the stray branch's parent was exactly the origin tip) and
  deleting the stray branch — no file damage (worker changed nothing). The worker brief's
  "Worktree step 0" warns about this but a worker can still mutate the shared checkout.
  Suggest the brief add an explicit guard: *never run `git checkout`/`git switch` outside
  your worktree path; verify `git rev-parse --show-toplevel` is your worktree before any
  branch op.* Possibly a kaizen item for the start-sprint worker brief.
