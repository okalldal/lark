# Sprint 20260627-1836 — residue ledger (ADR-0023)

Append-only record of the **irreducible** sprint residue: orchestrator/review
`RETRO:` notes and any synced-`master` SHAs. NOT a per-stage live ledger — the
staged table is reconstructed at finalize from the integration branch merge
history + child PR bodies. Worker `RETRO:` lives in each child PR body; parked
memos live on the issues; follow-ups are filed as issues.

- **Sprint base (`master`):** `79bb19331263302cc1020348f65cc61732159cb3`
- **Integration branch:** `sprint/20260627-1836`
- **Schedulable issues:** #485, #517, #518, #534, #543

## Synced `master` SHAs
(none — `master` did not move during the sprint)

## Orchestrator / review RETRO notes

### Wave 1 (#485→#546, #517→#548, #534→#547) — all 3 reviews `RETRO: none`

Orchestrator notes:
- **kaizen** — `ToolSearch "select:<name>"` for the GitHub MCP tools returned "No
  matching deferred tools found" at orchestrator startup (before any were loaded),
  not just when already-loaded as #314 documents. Keyword search worked. The §4 #314
  note frames this as an *already-loaded* quirk; it also bites on the *first* fetch.
  Suggest updating the §4 tooling note to say "use keyword search to load GitHub MCP
  tools; `select:` is unreliable for them both before and after load."
- **kaizen** — `mcp__github__list_issues` (full open backlog) and
  `actions_list list_workflow_runs` both overflow the tool-result token cap (165 KB /
  353 KB here) and must be sliced in a subagent / parsed via python. The §3 "build the
  plan from GitHub state" step should warn that the issue list must be fetched into a
  subagent (it already costs one) or paged, never read inline.
- Worker #534 (PR #547) flagged a reusable bignum-free decimal-compare helper that
  would also serve follow-up #545 (huge-count over-acceptance); that note is durable in
  the #547 body and #545 is filed — no separate residue needed.
- #485 review concurred with the issue author's request to **escalate** the tier despite
  by-rule `auto` (loader RC7/ADR-0013 blast radius) — staged, architect approves via omnibus.
