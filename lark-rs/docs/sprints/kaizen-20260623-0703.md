# Kaizen sweep `20260623-0703` — residue ledger

**Kind:** `/kaizen-sweep` (kit/governance omnibus, ADR-0018 + ADR-0022).
**Sweep base (`master` SHA):** `a2f5294efd12cfba65513b407ff6024877a2ea6e`
**Integration branch:** `kaizen/20260623-0703`
**Omnibus PR:** (filled in below once opened)

This is the append-only durability residue (ADR-0023): orchestrator + review-subagent
`RETRO:` notes and any synced-`master` SHAs. Worker retros live in their child PR
bodies; parked memos live on the issues; follow-ups are filed issues — none of those
are duplicated here. The *Staged* table is reconstructed at finalize from the branch
merge history, not maintained here.

## Backlog snapshot at sweep start

Schedulable (one child PR per coherent change):
- #309 — worker stranding → atomic review→push→PR→return, no background-spawn
  (`start-sprint.md` §4 worker brief + `finish-task.md`).
- #310 — verify named fix-site before `good-autonomous`
  (`triage.md` gate + `bug-hackathon.md` hypothesised/verified annotation).
- #311 — bake "keep ALL files inside your worktree" into the canonical worker brief
  (`start-sprint.md` §4).
- #312 — git-proxy fast-forward push rejection at finalize → push-retry + confirm-landed
  (`start-sprint.md` §7).
- #314 — sprint-#284 harness paper-cuts (4 items) (`start-sprint.md`). Bundled as one
  child PR (homogeneous low-risk doc notes); items enumerated for independent
  accept/reject — deliberate deviation from the one-concern-per-PR default, noted here.
- #315 — close-out must file every retro-flagged kaizen item
  (`start-sprint.md` §9 + `finish-task.md` + `kaizen-sweep.md`).

Parked `needs-decision` (never picked by a sweep):
- #313 — recurring shared-disk ENOSPC from concurrent worktree `target/` dirs; the
  architect must pick the durable mechanism (shared `CARGO_TARGET_DIR` vs disk-aware
  concurrency cap vs eager GC). Surfaced in the close-out parked inbox.

Triage-repair needed: none.
Re-triaged out of kaizen (product change): none.

## Wave plan

`start-sprint.md` is touched by 5 of 6 concerns (three of them the §4 worker brief), so
the `start-sprint.md` lane is serialized by staging-before-dispatching-next; the
disjoint #310 runs in parallel with wave 1.

- Wave 1: #309 (start-sprint §4 + finish-task) ‖ #310 (triage + bug-hackathon)
- Wave 2: #311 (start-sprint §4, off new tip)
- Wave 3: #312 (start-sprint §7)
- Wave 4: #314 (start-sprint paper-cuts)
- Wave 5: #315 (start-sprint §9 + finish-task + kaizen-sweep)

## RETRO residue (append-only)

### Wave 1–2 (staged: #318/#309, #319/#310, #320/#311)

- Review RETRO (#318/#309): none.
- Review RETRO (#319/#310): none.
- Review RETRO (#320/#311): two follow-up candidates surfaced, to FILE at close-out
  (the #315 discipline — do not drop retro-flagged kaizen items):
  1. The **optional second half of #311** (an orchestrator pre-push sweep for stray
     untracked `.rs` outside expected paths that fails loudly with a worker pointer) was
     correctly deferred from the pure-doc PR #320 — it touches `.githooks/`/`check-fast.sh`
     (code/CI surface). File as a kaizen follow-up so it is not lost when the omnibus
     closes #311.
  2. A **latent instance of #311's own class**: the worker's first Edit hit the *shared*
     checkout because the top-of-context system-reminder surfaces the shared `CLAUDE.md`,
     nudging a worker to Read shared paths first; the worktree-isolation guard fires on
     Edit but not on the misleading Read. Candidate kaizen follow-up (harness/brief).
- Orchestrator RETRO (waves 1–2): the staging-before-dispatching-next serialization of
  the `start-sprint.md` lane worked exactly as planned — every subsequent worker branched
  off a kaizen tip already carrying the prior §4 edits, so `pull --ff-only` fast-forwarded
  with **zero rebase conflicts** across #309→#311. Recommend the kaizen-sweep §3 note this
  serialize-by-staging pattern explicitly as the cheaper alternative to parallel-then-
  conflict-resolve when one file dominates the queue.

### Wave 3–4 (staged: #321/#312, #322/#314)

- Review RETRO (#321/#312): none.
- Review RETRO (#322/#314): kaizen-sweep §0's "one concern per child PR" reads as
  contradicting the deliberate bundle of #314's 4 homogeneous doc-notes. Resolved
  correctly here (each item enumerated for cherry-reject), but a future reviewer working
  only from §0 could flag it. **Candidate kaizen follow-up:** add a one-line §0 carve-out —
  multiple homogeneous low-risk doc-notes from a single paper-cut issue MAY bundle into one
  child PR when each is independently enumerated for cherry-reject. (FILE at close-out.)
- Worker RETRO (#321/#312 and #322/#314, both in their PR bodies): the kaizen/sprint
  worker brief says "pure-doc → SKIP check-fast.sh", but the repo's **pre-push git hook**
  still runs `cargo fmt --check` + `cargo test --all` on every push, so a doc-only push
  pays the full Rust gate cost anyway. **Candidate kaizen follow-up:** clarify the brief
  that "SKIP the gate" means *don't run it manually* — the pre-push hook fires regardless
  (and a stray untracked file or fmt issue can still block a doc push). (FILE at close-out.)
