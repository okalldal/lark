# Sprint 20260630-0c77248 — residue ledger

**Base `master` SHA:** `0c7724899276f23c279162e155b5ad5026e223b0`
**Integration branch:** `claude/start-sprint-1lmpth`
**Omnibus PR:** #573 (draft)

This is the append-only **residue file** (ADR-0023): the irreducible state with no
other durable home — the orchestrator's and review sub-agents' `RETRO:` bullets, and any
synced-`master` SHA. Worker `RETRO:` notes live in their child PR bodies; parked memos on
the issues; follow-ups as filed issues — those are **not** duplicated here.

The authoritative durable record is GitHub itself (the kept branch's merge history, the
child PRs, the labels). The omnibus body is reconstructed from those at finalize (§7); it
is not maintained per-stage.

---

## Plan (from GitHub state, base `0c77248`)

Two schedulable `good-autonomous` issues, blast-radius-disjoint → **one wave, two
concurrent workers**:

- **#230** — semantic-output C5 deterministic output-shape perf counters
  (`src/perf.rs`, new `tests/test_*output_counters*.rs` gate, `BENCH.md`). `prio:next`.
- **#561** — `%extend !` over a non-keep-all imported interior origin keeps tokens Python
  discards (`grammar/loader/compiler.rs`). `prio:next`.

Rest of backlog (67 open) is non-schedulable: 21 `needs-decision`, 4 `blocked`, 40
epics/untriaged/kaizen-without-DoD. #568 (last sprint's follow-up) lacks `good-autonomous`
+ a Files section → not schedulable this pass.

## Synced `master` SHAs

- base: `0c77248` (sprint start)

## Retrospective (orchestrator + review sub-agents)

### Wave 1

- **#561 → PR #575 (auto): STAGED** at sprint tip `7472219`. Verdict-only review: DoD PASS,
  differential-audit checkpoint satisfied (committed 8-cell matrix over imported-vs-same-grammar
  × explicit-`!` × keep-all/non-keep-all + Findings 2/3 pinned, adversarial rows audited vs
  Python 1.3.1), #493 opposite-direction fix confirmed non-regressed. Out-of-scope priority-discard
  find correctly split to follow-up **#574**. Review RETRO: none (PR body was the model the §4
  brief aims for); one note — the adversarial "beyond committed" differential rows live in PR prose,
  not as committed tests (acceptable; the 8-cell matrix carries the falsifiable load).
- **#230 → PR #576 (in review):** worker re-scoped to the two achievable assertions, deferring the
  zero-tree assertion to C8 — on the basis of an architect re-triage (dropped `needs-decision`,
  re-scheduled). Review in flight verifies that re-scope is architect-authorized, not unilateral.

### Orchestrator RETRO / kaizen (Wave 1)
- **K6 RECURS AGAIN (now 4th+ sprint): worker review-step stranding.** BOTH the #561 and #230
  workers independently hit the same conflict — the worker brief says "run `/code-review`
  INLINE/FOREGROUND, do not idle", but `Agent`/`/code-review` default to *background async*. #561
  read the code directly while awaiting; #230 blocked on `.done` sentinel files via a Bash
  `until` loop that hit the 10m cap (exit 143) but the completion notifications had already landed
  so nothing stranded. This is residue-K6/K2/K8 unresolved across sprints. Kit fix needed: either
  a first-class "await N background agents synchronously in one turn" primitive, or make
  `/code-review` foreground-blocking, or rewrite the brief to forbid background review entirely.
  Close-out must confirm a `kaizen` issue tracks this (don't re-file if #551/#552/#569–571 already do).
- **K7-recurrence: untriaged sprint follow-up.** #574 (split from #561) filed with `lark-rs`,`kind:bug`
  only — no `prio:`/`status:triaged`. Close-out `/triage` sweep must schedule it (same gap as last
  sprint's #561/#568).
- **Stale-body-after-re-triage (know-how / candidate kaizen).** #230's body still carried the
  architect-dropped unsatisfiable assertion even though `needs-decision` was removed and it was
  re-scheduled; the #230 worker had to reconcile body vs. labels vs. sprint-plan to recover the
  intended scope (the sprint-doc plan line was decisive). Suggests a triage/re-triage discipline:
  when an issue is re-scoped, edit the body's Done-when, not just the labels.

---

## Close-out (§7)

(filled at finalize)
