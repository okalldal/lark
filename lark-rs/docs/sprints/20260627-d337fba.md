# Sprint residue ledger — 20260627-d337fba

**Sprint base (master SHA):** `d337fba` (omnibus #488 merged to master).
**Integration branch:** `claude/start-sprint-e74b7s` (kept; durable record).
**Omnibus PR:** _(filled at creation)_

This is the append-only residue ledger (ADR-0023): the irreducible state with no
other durable home — the orchestrator's and review sub-agents' `RETRO:` bullets, and
any synced-`master` SHA. Worker `RETRO:`s live in their child PR bodies; parked memos
live on the issues; follow-ups are filed as issues. The *Staged* table is reconstructed
at finalize (§7) from the kept branch's merge history + child PR bodies — it is **not**
maintained live here.

## Plan (schedulable set — 11 issues)

| Issue | Cluster | Files | Wave |
|------|---------|-------|------|
| #506 | terminal | grammar/terminal.rs | 1 |
| #505 | loader   | grammar/loader/{compiler,imports}.rs | 1 |
| #496 | earley   | parsers/earley/mod.rs | 1 |
| #467 | terminal | grammar/terminal.rs | 2 |
| #449 | loader   | grammar/loader/terminals.rs | 2 |
| #348 | earley   | parsers/earley/* | 2 |
| #462 | terminal | grammar/terminal.rs | 3 |
| #445 | loader   | grammar/loader/tokenizer.rs | 3 |
| #457 | standalone | standalone/runtime.rs | 3 |
| #455 | lookaround | lookaround/{mod,lower}.rs | 4 |
| #391 | lexer    | lexer ordering / numeric terminals | 4 |

**Excluded / not scheduled this sprint**
- #478 (refactor lookaround/lower.rs) — body truncated, no parseable Done-when/Files → **triage repair needed** (also overlaps #455).
- ~12 triaged-but-not-`good-autonomous` issues → architect call.
- needs-decision inbox (23), status:blocked (6), epics (10), kaizen (off-cadence → /kaizen-sweep).

## Residue log (append-only)

_(orchestrator / review RETRO bullets and synced-SHA entries appended below as the sprint runs)_

### Wave 1 — staged 3/3 (#505→#511, #506→#512, #496→#513), all `auto`, omnibus #508

**Orchestrator RETRO (Wave 1)**
- Worker for #506 **stranded itself**: it spawned a review sub-agent and ended its turn waiting on a `.done` sentinel the harness never creates (the #309 anti-pattern). Recovered via `SendMessage` resume → it then pushed + opened PR #512 in one turn. The §4 brief *explicitly* warns against background-reviewer-then-idle, yet the worker still did it (its own RETRO admits this). Data point that the anti-stranding clause is being read but not reliably obeyed — kaizen: consider a harder guard (e.g. brief workers to NEVER spawn a background reviewer at all; only foreground `/code-review` or inline self-review). Relates to existing #502 / strand-mode reinforcement on #464.
- Verdict-only reviews repeatedly rendered while the child's `test` job was still `in_progress` (not failing). DoD/tier are content-final pre-green, but staging MUST gate on `test`+`wasm-binding` flipping to success — confirmed for all 3 before merge. The reviewers correctly flagged this each time; orchestrator honored it. Working as intended, noting the recurring pattern.

**Review sub-agent RETROs (verdict-only)**
- #511 review: verdict pre-green; orchestrator must gate on `test` (honored). Differential-audit checkpoint did its job (would have caught a banks-only submission; PR pre-empted with failed-first + multi-N oracle sweep).
- #512 review: praised **audit-pin honesty** (pins a known category-label divergence rather than hiding it) and the **git-stash before/after probe** as the falsifiable way to prove a divergence is pre-existing — reusable discipline for sprint audits. CI-in-progress note (honored).
- #513 review: RETRO: none. Independently re-derived the start-rule-never-transparent unreachability proof (load-bearing for the "identical-by-construction" refactor claim).

**Follow-ups filed during Wave 1 (product):** #509 (comment-in-brace over-rejection, pre-existing), #510 (quantified-bare-anchor over-acceptance, pre-existing) — both by the #506 worker, git-stash-proven pre-existing.

### Wave 2 — staged 3/3 (#467→#514, #449→#516, #348→#519), all `auto`, omnibus #508

**Orchestrator RETRO (Wave 2)**
- **No stranding this wave** — the reinforced "NEVER spawn a background reviewer then idle; foreground/inline only" clause in the briefs held for all 3 workers. The Wave-1 fix worked.
- Worker #348 (#519) hit **ENOSPC on the shared host filesystem** (another checkout's `target/` held ~5.4G) and could not run the full local gate, so it pushed with `--no-verify`. CI became the sole gate; orchestrator confirmed all 4 checks green before staging. Recurring infra issue — relates to existing kaizen #504 (parallel worktrees exhaust disk) / #313 (ENOSPC worktree target dirs); this sprint is another data point that worktree workers need a larger scratch fs or `target/` sharing.
- **`mcp__github__pull_request_read get_status` is misleading for this repo** — it reports `state: pending, total_count: 0` because CI runs as GitHub Actions *check-runs*, not legacy commit statuses. `get_check_runs` is the authoritative CI view. Orchestrator + #519 reviewer both hit this. Kaizen: add a one-line note to §5/§6 so a reviewer/orchestrator doesn't misread `get_status` as a hard pending/failure.

**Review sub-agent RETROs (verdict-only)**
- #514 review: RETRO none. Verified `Re==Re` byte-identical to the old `as_regex_str()` path; Hash/Eq consistency confirmed.
- #516 review: RETRO none. Independently re-ran the #449 differential + #286 residual cases through real Python Lark 1.3.1 — all matched; confirmed the residual decision was oracle-grounded (decide, not escalate).
- #519 review: two kaizen — (a) a verdict-only reviewer bridging a CI wait must poll via the **MCP `get_check_runs`**, not a bash/Monitor `curl` to api.github.com (proxy/auth returns empty); (b) same `get_status` total_count:0 gotcha as above. Confirmed the disproven-escalation claim is sound (architect re-triage comment 4815690018 + the oracle `use_cache` experiment).

**Follow-ups filed during Wave 2:** product — #517 (unify the 3 forest-DFS skeletons, by #348 worker), #518 (cyclic-explicit perf gate, by #348 worker); docs/governance — #515 (CLAUDE.md within-terminal-ordering doc note now imprecise, by #449 worker — correctly its own PR per §9).

### Wave 3 — staged 3/3 (#462→#520, #445→#521, #457→#522), all `auto`, omnibus #508

**Orchestrator RETRO (Wave 3)**
- **ENOSPC is the dominant infra friction of this sprint.** Workers for #445 (#521) and #457 (#522) both hit a 100%-full shared `/` mid-build; #457's worker had to delete *finished peers'* `target/` caches to make progress. Finished worker worktrees persist (branch at a non-tip commit) and each carries a multi-GB debug `target/`, starving later waves. Orchestrator pruned all 9 finished worktrees before Wave 4. **Strong kaizen** (supersedes/joins #504, #313): `/start-sprint` should set a shared `CARGO_TARGET_DIR`, or GC worker `target/` dirs between waves, or the orchestrator should `git worktree remove` finished workers as part of each wave's close. This is now a 3-sprint-running recurring failure (#284 → this).
- All 3 workers atomic, no stranding. #462 worker exceeded the issue's "re-bucket floor" to full normalize (oracle supported it). #457: #278/#402 confirmed already-closed in-process, so standalone parity is faithful mirroring, not scope-creep — reviewer verified.

**Review sub-agent RETROs (verdict-only)**
- #520 review: RETRO none. Audited the under-sampled neighbors (`max_width` sort key, `find_nothing_to_repeat`, `\N{NAME}` #461 ordering) against source — all clean, all pinned.
- #521 review: RETRO none. Verified the 3 name-shape predicates (#361 `>=2` underscores, #405 letterless, #445 `<=1`-underscore-then-nonletter) fire on disjoint sets — no double-reject.
- #522 review: two kaizen — (a) `get_files` MCP output for a diff-heavy PR (~62KB one line) overflowed the tool-result token cap, forcing a slice-and-summarize detour; §5 should tell a verdict-only reviewer to pull a large diff via a sub-agent up front; (b) §5 should say to re-poll `get_check_runs` once if a check is non-terminal rather than failing/deferring on a transient `in_progress`.

**Follow-ups filed during Wave 3:** none new (no out-of-scope finds beyond Wave 2's).

### Wave 4 — staged 2/2 (#455→#523, #391→#524), all `auto`, omnibus #508

**Orchestrator RETRO (Wave 4) + finalize**
- **Cross-worktree contamination (new kaizen).** The #391 worker (#524) ran `generate_oracles.py`, which wrote the regenerated `python_numbers` oracle JSON + `test_oracle.rs` + `generate_oracles.py` into the **shared checkout** (4 files staged in the orchestrator's index), not only its worktree. The Stop hook caught the dirty tree; orchestrator confirmed the 4 files were byte-identical to PR #524's branch and `git restore`d them (no work lost — #524 is canonical). Separately, the #455 worker's pre-push sweep caught a stray untracked `tests/zz_stack_probe.rs` it "did not create this session". Two contamination signals in one wave → file a kaizen: worker file ops (oracle regen + scratch probes) must be isolated to the worktree; the shared checkout must stay clean.
- master (`d337fba`) never moved during the sprint → no `origin/master`-into-sprint merge needed; master is a confirmed ancestor of the integration branch.
- One transient `git rev-parse origin/<branch>` "Needed a single revision" error coincided with a concurrent worker push (ref lock); resolved on retry. Minor.

**Review sub-agent RETROs (verdict-only)**
- #523 review: RETRO none. Verified `parse_paren` is the sole recursion gate, 250/251 boundary exact, depth balances on all exit paths, all 4 callers fall back to None; confirmed no contamination files in the diff.
- #524 review: RETRO none. Confirmed the oracle burndown is genuine (`oracle_contradictions.json` 5→0, the ADR-0030-sanctioned outcome), parity real (Python-recorded token types, no self-comparison), failed-first honest.

**Follow-ups filed during Wave 4:** none product. (Kaizen follow-ups filed at close-out — see below.)

## Finalize — staged table reconstruction (all 11 `auto`)

| Issue | Child PR | Squash | Tier |
|------|---------|--------|------|
| #505 | #511 | a0abd7e | auto |
| #506 | #512 | 2d1819b | auto |
| #496 | #513 | 2237d56 | auto |
| #467 | #514 | 72782c7 | auto |
| #449 | #516 | 456944b | auto |
| #348 | #519 | 5178deb | auto |
| #462 | #520 | f20a76f | auto |
| #445 | #521 | 0ed8389 | auto |
| #457 | #522 | 1302971 | auto |
| #391 | #524 | b3f841e | auto |
| #455 | #523 | dcb669d | auto |

**Kaizen follow-ups filed at close-out (§9 / ADR-0032):**
- **#525** (new) — oracle regeneration (`generate_oracles.py`) writes into the SHARED checkout from a worktree-isolated worker (the #391/#524 contamination caught by the Stop hook).
- **#526** (new) — CI-polling guidance for verdict-only reviewers + orchestrator: `get_check_runs` is authoritative (`get_status` reports `total_count:0`); re-poll once on `in_progress`; pull diff-heavy `get_files` via a sub-agent.
- **#504** (already-tracked) — ENOSPC / un-pruned worktree `target/` dirs. Added this sprint's 3rd-recurrence data point (comment).
- **#464** (already-tracked) — worker backgrounds its pre-PR review and strands work (#506 recurred), + stray-probe contamination (`zz_stack_probe.rs`). Added data point (comment).

Zero-kaizen would have been conspicuous; this sprint surfaced 4 kit items (2 new, 2 recurrences), all filed/tracked.

## Post-finalize — architect extended review of #508 (2026-06-27)

Architect did a static extended review of #508 + all 11 children: **approved, no respin, merge as-is**; 4 non-blocking items, all now tracked as `good-autonomous` follow-ups (none folded into the omnibus — architect's call was post-merge):
- Rec 1 (#511): `checked_sub`/assert instead of raw `total - preexisting.len()` → **#527** (prio:later).
- Rec 2 (#519): keep the cyclic-explicit Earley perf gate alive → **#518** (bumped prio:next).
- Rec 3 (#522): `Default` Meta with `empty:true` + document generated `Tree`/`Token` struct-shape change + fix `%ignore` comment → **#529** (prio:later).
- Rec 4 (#514): soften hash-collision test wording to the real `Eq`/`Hash` contract → **#528** (prio:later).

Disposition posted as a comment on #508. #508 left untouched, ready for the architect's merge.
